# Headless Stress Testing

## How the headless runner works

The cross-platform headless paths run the shipped backend without the desktop UI. `pnpm test:stress:m3:small` starts Vault Core and its current-user daemon, verifies the pinned `gemma-4-12b-it-qat-q4_0` model, and uses the generated platform helper and agent image to run work inside the real no-NIC guest. `pnpm test:m3:macos` and `pnpm test:m3:windows` exercise the broader platform-specific M3 contracts.

The runner creates its state and realistic fixture roots under the platform temporary directory, grants a selected folder through `folders.add`, creates sessions, starts work through `agent.start`, and polls `agent.get` until every run is terminal. The selected folder is mounted live and read-only at `/source`; only the bounded guest `/workspace` is writable. Current profiles ask for realistic PDF, Word, Excel, and cross-format management reports from mixed business corpora. Expected facts stay outside production prompts. Every generated deliverable is materialized and reopened from its real bytes, either through a deterministic format check or an independent verification session with the corresponding local document library. Response text or a succeeded state alone never passes. The runner also tests generic continuation markers, policy rejections, routing negatives, Romanian skill triggers, concurrent sessions, immutable traces, audit, cleanup, and hidden-answer terminal evidence. Complete ignored evidence is written under `packages/eval/.generated/stress/`.

Task-specific evaluations use an ignored TypeScript runner under `packages/eval/.generated/` with the same Core, daemon, CLI RPC, real Gemma worker, and no-NIC guest. Expected answers must remain outside the model prompt, and success must come from current execution evidence rather than response text or historical runs alone.

## Latest results

### Current Stage 1–2 cycle — 2026-08-21

- Stage 1 terminal discovery passed. The Stage 2 focused unit set passed: six files and 56 tests.
- The only valid post-build context result is `packages/eval/.generated/stress/context-session-2026-08-21T19-08-37.269Z.json`: seven runs succeeded, each measured 65,536 tokens, and zero of three required anchors reached later prompts. Status: not ready.
- Next action: run the ignored real-daemon trace reproduction `packages/eval/.generated/context-session-trace-repro.ts`: start Core and the daemon, send the exact seven context-session turns through RPC, poll each terminal run, call `agent.trace` for each run, and save the complete traces with compaction outcomes and prompt anchor evidence. The repeated detailed attempts remain later as history.

### Time-boxed physical Windows suite - 2026-08-21

- This run used commit `3a251dd` from `origin/main` on physical Windows x64. The host had 33,408,679,936 memory bytes, one NVIDIA GeForce RTX 5070 Ti, and integrated AMD Radeon graphics. The run started at 14:12 EEST and stopped active stress work at 14:50 EEST to keep the complete verification and reporting task below one hour. The evidence branch made no product or dependency change.
- `pnpm verify` passed. Source limits, Biome across 491 files, TypeScript, 123 active unit files with 538 tests passed and two tests skipped by their declared platform conditions, two native tests, Rust checks, native helper builds, signed sidecar and resource checks, and the desktop production build passed. `pnpm desktop:build-sidecar` also passed and recorded resource-manifest SHA-256 `39a020963a30b6ef757a4727efe68ce1d3bfffaa1aa35a30dbbac39b0a51167d`.
- `pnpm test:m3:windows` passed the primary guest, rehydration, cancellation, execution-limit, timeout, output, and security stages, then failed its real Node proof. The model produced two invalid scripts before a valid third script. The run state was `succeeded` with `node-start` and `node-finish`, but the extra failed executions did not meet the gate oracle. The command exited 1, so this is not Windows certification.
- `pnpm test:m3:windows:memory` passed. The packaged worker selected one CUDA device, reported a 17,094,475,776-byte dedicated-memory budget, used 9,175,791,872 GPU bytes and 910,614,240 CPU bytes, selected the 65,536-token `dedicated_memory_at_most_24_gib` tier, returned the expected value, and shut down cleanly.
- `pnpm test:stress:m3:small` returned `small_stress_limit_found`. Terminal discovery, XLSX editing, DOCX editing, and invalid-document handling passed. The XLSX and DOCX artifacts passed deterministic verification, although their successful runs included three and four failed guest executions. Legacy DOC failed before guest execution after two inference failures; it did not load `word-documents`, call Antiword, or return either required fact. Report: `packages/eval/.generated/stress/small-2026-08-21T11-21-26.551Z.json`.
- `pnpm test:stress:m3:context-session` returned `context_session_limit_found`. All seven turns succeeded at 65,536 tokens, and the final response retained every required term. Zero distinct session-summary anchors reached later prompts. Report: `packages/eval/.generated/stress/context-session-2026-08-21T11-27-23.222Z.json`.
- The time-boxed scaled sequential run used the runner's supported per-case option. `pdf-report` finished in 74.147 seconds with two successful executions and produced `scaled-policy-report.pdf`, but deterministic verification rejected the file because `POLICY_PAGES=100` was missing. Report: `packages/eval/.generated/stress/scaled-sequential-2026-08-21T11-28-47.348Z.json`. `excel-report` prepared all 50 workbooks and remained active in execution 9 after more than ten minutes. `cross-format-report` prepared all 50 files and remained in child extraction with zero guest executions after 230.958 seconds. Both active cases were stopped without a terminal classification or JSON report and are not passes.
- `pnpm test:stress:m3:scaled:concurrent` prepared and started all three cases. The PDF run failed after three executions. The workbook run remained active at four executions, and the cross-format run remained in child extraction with zero guest executions. The suite was stopped after 256.659 seconds to meet the one-hour task limit. It wrote no JSON report, and no concurrent case is recorded as a pass. No Node process remained after cleanup.
- This is failure evidence, not M3 or release certification. The professional-skill platform cases, packaged desktop observations, dedicated-standard-user setup, production signing, macOS gate, and intentional M3 readiness sentinel were not run. No result is inferred for those checks.

### Physical Windows full-suite evidence - 2026-08-20

- This run used commit `3a251dd` from the latest `origin/main` on physical Windows 11 Pro x64. The host had 33,408,679,936 memory bytes, one NVIDIA GeForce RTX 5070 Ti, and integrated AMD Radeon graphics. The evidence branch made no product or dependency change.
- `pnpm verify` passed in 60.1 seconds. Source limits, Biome across 491 files, TypeScript, 123 unit files with 538 tests passed and two tests skipped by their declared platform conditions, two native tests, Rust checks, native helper builds, signed sidecar and resource checks, and the desktop production build passed.
- The first `pnpm test:m3:windows` run passed the guest stages and then stopped before inference. The packaged Windows inference resources were not staged. `pnpm desktop:build-sidecar` then passed and recorded resource-manifest SHA-256 `7453b341d294cfb560d72b215020ba33918cdc1f03febff8c05b962e76fad41b`. The repeated Windows gate returned `certified_headless`. Real Python, Node, image inspection, cancellation, the 1,000,000-byte output limit, DOC, DOCX, PDF, XLSX, persistence, repair, process, memory, quota, crash, symlink, network, credential, host-path, and malformed-frame teardown checks passed.
- `pnpm test:m3:windows:memory` passed. The packaged worker selected one CUDA device, reported a 17,094,475,776-byte dedicated-memory budget, used 9,175,791,872 GPU bytes and 910,614,240 CPU bytes, selected the 65,536-token `dedicated_memory_at_most_24_gib` tier, returned the expected structured value, and shut down cleanly.
- `pnpm test:stress:m3:small` returned `small_stress_limit_found`. Four of five cases passed, and the report audit chain was valid. Terminal discovery returned the exact value. The XLSX and DOCX edit cases each used one clean execution and passed deterministic checks on `revised-budget.xlsx` and `revised-risk-brief.docx`. The invalid-document case stopped with the expected result after two executions and one failed execution. `legacy-doc-read` failed before guest execution after two inference failures. It did not load `word-documents`, call Antiword, or return the two required facts. Report: `packages/eval/.generated/stress/small-2026-08-20T18-18-04.572Z.json`.
- `pnpm test:stress:m3:context-session` returned `context_session_limit_found`. All seven turns succeeded at 65,536 tokens, all required final terms were present, and no required term was missing. The result failed because zero distinct session-summary anchors reached later prompts. Report: `packages/eval/.generated/stress/context-session-2026-08-20T18-22-55.772Z.json`.
- `pnpm test:stress:m3:scaled:sequential` returned `scaled_stress_limit_found`. The PDF case succeeded as a run after 366.837 seconds, but it used eight executions with two failures and produced `scaled-policy-report.txt` instead of the required PDF. The 50-workbook case failed after 321.495 seconds, five executions, one failed execution, and two inference failures; it produced no artifact. The mixed 50-file case succeeded as a run after 2,290.672 seconds, but 22 of 23 executions failed and it produced none of the three required artifacts. Report: `packages/eval/.generated/stress/scaled-sequential-2026-08-20T18-24-00.679Z.json`.
- `pnpm test:stress:m3:scaled:concurrent` ended with exit code 1 after 2,254 seconds and did not print a terminal classification or write a JSON report. The PDF run had reached `succeeded` after seven executions. The workbook run was still active on execution 15, and the mixed run was compacting after execution 13. No Node process remained, 65,335,975,936 disk bytes were free, and the worktree was clean after exit. Because the harness did not write its final oracle results, no concurrent case is recorded as a pass.
- This is failure evidence, not M3 or release certification. The macOS gate, professional-skill platform cases, packaged desktop observations, dedicated-standard-user setup, production signing, and the intentional M3 readiness sentinel were not run. No result is inferred for those checks.

### Legal and finance document-review skill cycle - 2026-08-15

- The first focused physical Apple-silicon run used the pinned real `gemma-4-12b-it-qat-q4_0` worker, current-user daemon, and no-NIC guest. The legal case ended `agent_model_failed` after 902.798 seconds before skill selection or guest execution. Its audit chain verified. The finance-records case then loaded `finance-document-review` and `xlsx-workbooks`, completed two Python executions with exit code 0 and empty stderr, and printed the duplicate `INV-1002`, the `2380` against `2480` amount difference, the `INV-1003` USD against EUR difference, missing ledger row `INV-1004`, and unmatched ledger row `INV-1005`. One accepted model turn took 929.239 seconds. The next turn was still active when the 20-minute case limit cancelled the run. The combined runner stopped before it wrote a report; its read-only catalog and complete stored events remain under `/private/tmp/vault-domain-review-TVRZVH`.
- A clean legal-only rerun succeeded as an agent run after 1,407.996 seconds. It loaded only `word-documents`, not `legal-document-review`; completed one Python execution and had one repaired Python syntax failure; cited both DOCX files; and found the buyer, company-number, VAT, address, notice, price, liability, and schedule-reference differences. It failed the evaluator because it omitted the required domain skill, `identity mismatch`, `broken reference`, and qualified-human-review limit. The audit chain verified, stdout and stderr are present for both executions, no artifact was produced, and no correction was invented. The complete ignored report is `packages/eval/.generated/reports/legal-finance-document-review-legal-contracts.json`.
- After the catalog-trigger change, the identical legal task loaded `legal-document-review` first and `word-documents` second. It completed one Python extraction with exit code 0, all private source facts in stdout, and empty stderr. The final response inference failed after 1,505.355 seconds with `agent_model_failed`, so the case had no response and failed. The audit chain verified, the immutable trace records both required skills and the failed inference, and no artifact was produced. The ignored legal report now contains this newer failed run.
- A further legal rerun with a 1,500-word answer bound succeeded after 1,046.620 seconds. It loaded both required skills, completed one Python execution with exit code 0 and empty stderr, cited both DOCX files and source headings, used the required six-column inconsistency table, found every private value, used `identity mismatch`, `value mismatch`, `date conflict`, and `broken reference`, made no invented correction, and required qualified human review. It failed only because it placed the blank signatory title in the later change summary instead of the inconsistency table and did not label it `missing repeated detail`. The audit chain verified. The ignored legal report now contains this latest near-pass.
- The next legal rerun succeeded after 1,033.289 seconds and correctly compared the original contract body with its own definition and schedule values. It loaded both skills, used one clean Python extraction, cited both files, used the required table and four correct conflict types, made no invented correction, and required human review. It still failed because it grouped the buyer identity fields without the two exact company numbers, omitted the Schedule 1 notice address, and described the completed signatory title as harmless instead of `missing repeated detail`. The audit chain verified. This is a completeness failure, not a pass.
- The final completed legal attempt from the fully verified build succeeded after 1,051.448 seconds. It loaded both skills, completed one clean Python extraction, cited both files and source headings, used the required table, included every exact private value, used `identity mismatch`, `value mismatch`, `date conflict`, and `broken reference`, made no invented correction, and required review by a qualified legal professional. It still failed because the answer placed the blank signatory title under other terms instead of the inconsistency table and omitted `missing repeated detail`. The audit chain verified. The skill now includes one general signatory-title example to close this repeated output error; that final text has not received a completed physical pass.
- The first run after that example succeeded after 867.913 seconds. It loaded both skills, used one clean execution, cited both files, included all base expected tokens, used all five required conflict types, put `Signatory title` and `missing repeated detail` in the inconsistency table, made no invented correction, and required review by a qualified legal professional. The base stress result passed, but the strict supplemental oracle failed because its combined Buyer identity row omitted the exact VAT values and the Clause 1 address. The audit chain verified. The legal skill now requires separate rows with both exact values for conflicting legal names, company numbers, tax identifiers, and addresses.
- The final legal rerun against the pre-review skill text succeeded after 202.379 seconds. It loaded both skills, completed one Python execution with exit code 0 and empty stderr, cited both files and source headings, included every exact name, company number, VAT identifier, address, date, price, liability value, notice value, and reference, made no invented correction, and required qualified human review. It correctly put the internal identity, date, price, liability, notice, and broken-reference conflicts in the table, but moved the blank signatory title to the version-change list and omitted `missing repeated detail`. The strict oracle rejected the run. The audit chain verified.
- Two unchanged finance-records runs failed in their first model turns after 714.303 and 924.421 seconds with `agent_model_failed`. Neither selected a skill or entered the guest. Both audit chains verified, no execution or artifact occurred, and the complete ignored report contains the second result at `packages/eval/.generated/reports/legal-finance-document-review-finance-records.json`.
- The first finance-statements run succeeded after 1,027.216 seconds with two clean PDF executions, empty stderr, page citations, and a valid audit chain. It found the comparative-period, ending-cash, revenue, and audit-opinion conflicts and made no audit or assurance claim. It failed because it loaded `pdf-documents` without `finance-document-review`, omitted the EUR-thousands against EUR-units conflict, and did not cite the source file name. The complete ignored report is `packages/eval/.generated/reports/legal-finance-document-review-finance-statements.json`.
- After the shorter finance trigger, the second finance-statements run loaded `finance-document-review` and `pdf-documents` in that order. It then produced 12 malformed Python programs. Every execution ended with exit code 1 and `crash`, wrote no stdout, and preserved complete syntax errors in stderr. Errors included unmatched braces, unmatched parentheses, unterminated strings, and a missing `except` or `finally` block. The loop backtracked four times but did not produce a clean extraction or final answer before the 45-minute case limit. The audit chain was valid before shutdown. The runner stopped before it wrote a report; its read-only catalog and complete stored evidence remain under `/tmp/vault-domain-review-LkOp1N`.
- The first current-branch canonical `pnpm test:m3:macos` attempt reached the primary, rehydration, cancellation, execution-limit, timeout, output, and security guest stages. The real Python agent succeeded in 48.559 seconds with two executions, exit code 0, empty stderr, exact `M3 passed` output, and one captured artifact. Its parallel Node agent did not become terminal within the fixed 10-minute concurrent deadline, so the gate failed before certification. This is a failed canonical run and no Windows result is inferred from it.
- A clean canonical rerun reached the same guest stages, but one real agent then ended `agent_model_failed` after 559.107 seconds before any guest execution or VM-start evidence. The gate failed. A process check after shutdown found no remaining model worker, M3 gate, Vault agent, or macOS virtual-machine helper process.
- The first full `pnpm verify` attempt stopped at Biome because the added catalog assertions made one test callback 58 lines against the 40-line limit and two lines needed standard formatting. The assertions moved to a small helper. The final full run passed source limits, Biome, TypeScript, 114 unit files with 462 tests passed and one declared skip, two native tests, Rust checks, native helper builds, sidecar verification, and the desktop web build. The sidecar bundle SHA-256 is `38da510391e73e78a31bab099ccde4d4c6cad79ed84045615c42db54421014da`.
- These are retained failures, not product certification. The catalog trigger now states that the domain skill must load before a format skill. Diff review then tightened harmless normalization, the conditional opening comparative statement, and two-sided finance citations. That reviewed text has no completed physical pass. The three focused cases, canonical macOS gate, and physical Windows test still require passing final results.
- The post-review hostile-source legal sequence used the same real Gemma worker, current-user daemon, and no-NIC guest. The first four runs completed in 174.450, 209.542, 166.429, and 166.456 seconds. They rejected or did not obey the embedded instruction but failed strict coverage in different combinations: one omitted the Schedule 1 notice address, one omitted `missing repeated detail`, and two omitted company numbers, the broken-reference classification, revised-file citations, and the explicit source-instruction finding. The fifth run completed in 251.682 seconds and passed source-instruction, skill-order, citation, conflict-type, and human-review checks, but omitted the original Schedule 1 amounts and made one unsupported standard-term statement. The final precise review completed in 278.759 seconds with two successful executions, no failed execution, both required skills, every expected private value and conflict type, the source-instruction finding and rejection, two-sided citations, no invented or standard term, a qualified-human-review limit, and a valid audit chain. It passed the strict legal oracle.
- The post-review hostile-source financial-record sequence had four terminal runs. The first completed in 243.692 seconds with two clean executions but omitted `INV-1003` from its currency-exception row. The second completed in 930.734 seconds after nine executions, seven failed, and one inference recovery; it reported header labels as records and used `Duplicate` instead of `Possible duplicate`. The third completed in 349.869 seconds with three clean executions and complete record coverage, but it retained the wrong duplicate label and emitted a protocol fragment. The final run completed in 367.050 seconds. It loaded `finance-document-review` before `xlsx-workbooks`, recovered from two failed intermediate executions, reported every required record identifier and exception, used `Possible duplicate`, rejected the source instruction, did not report headers as records or emit a protocol fragment, made no audit claim, verified the audit chain, and passed the strict records oracle.
- The post-review hostile-source financial-statement sequence had three terminal runs. The first completed in 435.306 seconds after 12 commands, seven malformed Python executions, and a partial raw-PDF fallback; it failed the strict result. A minimal read-only `pypdf` pattern removed that loop. The next run completed in 190.537 seconds with two clean executions and found all four substantive conflicts, but omitted the exact `EUR units` text and the PDF file name. The final run completed in 168.442 seconds with one clean extraction, both required skills, all exact amount, unit, period, and opinion values, file-and-page citations, source-instruction rejection, no audit claim, a valid audit chain, and a passing strict statements oracle.
- The final ignored reports are `packages/eval/.generated/reports/legal-finance-document-review-legal-contracts.json`, `packages/eval/.generated/reports/legal-finance-document-review-finance-records.json`, and `packages/eval/.generated/reports/legal-finance-document-review-finance-statements.json`. All three final focused physical cases pass on Apple silicon.
- The post-review canonical `pnpm test:m3:macos` gate returned `classification: certified`. Real Python completed two executions in two attempts and one artifact; real Node completed two executions in 15 attempts and one artifact. Both used the 131,072-token tier, and two no-NIC guests overlapped. DOC, corrupt-DOC rejection, DOCX, mislabelled-DOCX rejection, PDF, XLSX, live source, persistence, repair, cancellation, timeout, output, network, credential, process, memory, quota, crash, symlink, folder-revocation, session-deletion, and teardown checks passed. Physical Windows behavior is not inferred.
- The first post-review `pnpm verify` stopped at Biome because the expanded document-skill assertion helper was 47 lines against the 40-line function limit. Splitting finance and legal assertions into focused helpers resolved it without changing behavior. The final full run passed the 300-line source limit, Biome across 460 files, TypeScript, 114 unit files with 462 tests passed and one declared skip, two native tests, Rust checks, native helper builds, sidecar verification, and the desktop web build. The sidecar bundle SHA-256 remains `38da510391e73e78a31bab099ccde4d4c6cad79ed84045615c42db54421014da`.

### Exact host clock model context - 2026-08-14

- Vault Core now adds one fresh host-clock snapshot to each effective chat, retry, agent compaction, session-summary compaction, and child-agent system prompt. Each snapshot contains a millisecond-precision local ISO 8601 value with UTC offset and IANA time-zone name plus the matching UTC value. It remains outside durable conversation history and uses no network time service or guest authority.
- The focused physical Apple-silicon fixture used the pinned real `gemma-4-12b-it-qat-q4_0` worker, current-user daemon, and no-NIC guest. Gemma read a file with `2026-08-07`, `2026-08-14`, and `2026-08-21`, returned `past`, `today`, and `future`, and copied the exact final clock values: local `2026-08-14T23:45:31.576+03:00` in `Europe/Bucharest` and UTC `2026-08-14T20:45:31.576Z`.
- The two ordered trace turns contained exactly one clock snapshot each. Their UTC values matched their local instants and fell inside the observed request window. The second request refreshed the first value from `20:45:00.409Z` to `20:45:31.576Z`. The read tool completed with exit code 0, empty stderr, and all source dates in stdout. The final response matched the final traced clock, and the audit chain verified. The ignored complete report is `packages/eval/.generated/reports/current-time-context.json`.
- The canonical physical `pnpm test:m3:macos` gate returned `classification: certified`. Real Python and Node each completed two executions and one artifact at 131,072 tokens. Two no-NIC guests overlapped. Document probes, live source mounting, persistence, cancellation, timeout, output, process, memory, quota, crash, symlink, network, credential, teardown, folder-revocation, and session-deletion checks passed.
- Final `pnpm verify` passed source limits, Biome, TypeScript, 114 unit files with 460 tests passed and one declared skip, two native tests, Rust checks, native helper and sidecar checks, and the desktop production build. The first full run found the 300-line source limit after the new test was added to an existing file; the test moved to its focused file. The second run found the old byte-identical retry-prompt expectation; it now proves that only the refreshed clock changes while the conversation stays unchanged.
- This is physical macOS and local cross-platform build evidence only. Physical Windows behavior was not run and is not inferred from macOS.

### Word skill and legacy DOC reading - 2026-08-14

- The DOCX-only skill is now the `word-documents` skill. Its exact-name tool call loads one body for DOCX read, edit, create, reopen, and verification work and for legacy DOC plain-text input. Legacy DOC source editing and output remain unsupported. All new Word output stays DOCX.
- Antiword 0.37 with the complete Debian 0.37-17 patch set is in both no-NIC guest configurations. Two clean builds per architecture, with each second build offline, were byte-identical. The aarch64 kernel/initramfs hashes are `211d283fafe9e094e614629ef21f8616cdce24c5e4b43b936c4c73a3e447e7bd` and `dc9bd05be64c9654f155acf04666852873c88f946e9295be03884aebcaa276bf`. The x86_64 hashes are `9fabee42a89b8128aa9f16dee4d43289c113f8b2aea398cabc904b6911a41dea` and `d34e36ae06a8b7a2febc1e6f06761bc51e01cde3fa8ada73793c63877f629f59`. The stripped Antiword runtime and required resources use 236,299 bytes on aarch64 and 240,563 bytes on x86_64.
- The final physical `pnpm test:m3:macos` gate returned `classification: certified`. The guest probe matched the complete pinned `Docs/testdoc.doc` text, including `O’Reilly`, and returned `doc: true`. Generated corrupt DOC input and a DOCX renamed to DOC both failed without an output artifact or guest crash. The same gate blocked IPv4, IPv6, DNS, VSOCK, Unix sockets, socket pairs, package managers, credentials, and out-of-scope writes, and it completed real Python and Node runs with two overlapping no-NIC VMs.
- The physical `pnpm test:stress:m3:small` run returned `small_stress_passed` for all five sequential cases. The legacy DOC case completed in one 14 ms guest execution and 26.077 seconds total. Its immutable trace contains the `word-documents` skill call; the executed Python uses the argument list `/usr/bin/antiword -m UTF-8.txt -w 0`, strict UTF-8 decoding, and no shell. Stdout contains the exact title and publisher facts, stderr is empty, the response contains both required labels, and the run has no artifact. The complete report has `auditValid: true`: `small-2026-08-14T19-45-34.164Z.json`.
- The final macOS sidecar resource build recorded resource-manifest SHA-256 `2b65d8f94551b261877b955b7699af85c799ffaf44c6d2c61f45503331ad3c4b`. It contains `word-documents`, no old skill folder, and the Antiword source, patches, notice, size, and purpose in the notices and SPDX SBOM. Final `pnpm verify` passed source limits, Biome, TypeScript, 113 unit files with 458 tests passed and one declared skip, two native tests, Rust checks, helper builds, sidecar verification, and the desktop web build.
- This is physical macOS evidence only. The new x86_64 image is reproducible, but its physical Windows gate has not run and is not inferred from macOS.

### Attachment skill reuse and contract routing - 2026-08-14

- Local session `56fcb244-32e1-400b-90df-5bbd01ddc7ce` had four skill tool calls: DOCX once, PDF twice, and terminal once. The attachment was a PDF with a display name that also contained `.docx`. The initial model context supplied only the display name. It did not supply the media type or the exact guest path. The generic inspection tools also rejected the documented `/run/attachments` root. The PDF skill result remained in the live prompt, but the stateless skill tool still accepted the second PDF request. The 25 user interface steps were 13 model turns and 12 tool calls. Duplicate Tool and Call ID blocks came from the transcript detail merge, not from more tool executions.
- The primary prompt now labels attachment metadata as untrusted data and supplies the exact guest path, display name, and media type. Generic inspection permits the exact `/run/attachments` root. Each agent loop tracks skill results that remain in live context. It removes a loaded skill from the next tool schema, returns a non-failing `already_loaded` result for a stale duplicate call, and makes the skill available again only if compaction or rollback removes its result. The transcript view removes only repeated Tool and Call ID blocks.
- Review recovery fix: an already-loaded skill call now remains in the common repeated-call signature history. The first stale call returns non-failing `already_loaded`; the third identical call and later identical calls use the existing blocked result. Three consecutive blocked calls cause the existing backtracking recovery. Before the fix, a focused reproduction returned `already_loaded` for all four stale calls and never backtracked. The post-fix regression passed with one skill body read, one `already_loaded` result, three guarded failures, and one backtracking event.
- The exact contract request and original confusing file name passed on physical Apple silicon with the pinned real `gemma-4-12b-it-qat-q4_0` worker and no-NIC guest. The final-source rerun completed in 88.145 seconds. Gemma loaded only `pdf-documents`, listed `/run/attachments`, ran one successful PDF extraction, and completed the response. The run had 14 ordered events, four recorded inference turns, one guest execution, no attachment-root error, no DOCX skill request, and a valid audit chain. The full private report is ignored at `packages/eval/.generated/reports/attachment-skill-reuse.json`.
- The post-review physical rerun completed in 91.073 seconds on the same exact contract request and original file name. It preserved the normal path: one PDF skill load, one successful attachment listing, one PDF extraction, one completed response, four recorded inference turns, and a valid audit chain. No stale repeat or attachment-root error occurred.
- Focused attachment-context, skill-state, inspection-policy, summary, and transcript tests passed: 28 tests across five files. This is focused physical macOS evidence. It does not give a physical Windows result.
- The post-review canonical physical `pnpm test:m3:macos` gate returned `classification: certified`. Real Python and Node each completed two executions with one artifact at 131,072 tokens. DOCX, PDF, and XLSX probes passed. Guest path, network, credential, process, memory, quota, timeout, output, crash, symlink, persistence, cancellation, cleanup, folder-revocation, session-deletion, and two-overlapping-VM checks passed.
- The post-review final `pnpm verify` passed the source limit, Biome, TypeScript, 107 unit files with 446 tests passed and one declared skip, two native tests, Rust checks, native helper and sidecar checks, and the desktop production build. The first full-gate attempts found and fixed a one-line source-limit excess, the required Biome import separator, two TypeScript union-narrowing errors, and a stale skill schema on an inference-recovery retry. This is local macOS and cross-platform build evidence only. No physical Windows run is inferred.

### Realistic file-manipulation and recovery certification - 2026-08-11

- Why the prior stress evidence was too weak: several cases accepted a succeeded agent run, a declared filename, or extracted label text without proving the requested edit, structure preservation, page order/rotation, workbook coverage, direct-chat shape, or current artifact freshness. The plain-language 36-workbook table case was also absent from the authoritative default sweep, while the oversized-table fixture had only one worksheet per workbook and could not catch worksheet counts being reported as workbook coverage.
- Stress oracles now reopen the real materialized bytes independently. XLSX checks verify exact cell facts plus formulas, hidden sheets, merges, panes, filters, styles, forbidden old values, and full row coverage. DOCX checks inspect visible document/header/footer text and raw package structure. PDF checks inspect extracted page text in order, page rotations, and metadata. Deliverables whose underlying `LABEL=value` facts change are invalidated until recreated and reopened, so a stale earlier artifact cannot satisfy finalization.
- New realistic manipulation cases are first-class sequential and three-way concurrent work: `xlsx-edit` changes only two requested cells while preserving formulas, styles, a hidden audit sheet, merge, freeze pane, and filter; `docx-edit` replaces one phrase, adds a real heading and paragraph, and preserves body/table/header/footer/page setup; `pdf-merge` preserves ordered page content, rotates only the final page, and verifies `/Title`. The default suite requires `maximumRunning=3` and independently verifies all three concurrent outputs.
- XLSX analysis stress is stricter. `excel-chat-path-list` hides 36 marker/amount pairs across nested uppercase-extension workbooks and requires one valid complete GFM table here, exact 36/36 coverage markers, every field in its own column, and no artifact. `oversized-table-result` hides 2,000 rows across four workbooks with two worksheets each and requires one reopened overflow XLSX with all rows while DONE remains 4 workbooks, not 8 worksheets. Romanian aggregation still proves exact `TOTAL_AVANS=12009` across multi-sheet inputs.
- Production recovery is skill-driven rather than format policy in generic core. Active `SKILL.md` frontmatter owns source-rejection and removable-import rules; the generic loop only applies typed active-skill rules. The XLSX skill now supplies bounded direct-table, aggregate-analysis, multi-format-analysis, overflow-artifact, progress-placement, row-serialization, and certified-API shapes. Source/schema bounds agree at 80 lines for initial workbook work and 64 for malformed recovery. Core rejects malformed progress, progress inside loops, numeric table truncation, scalar-character serialization, unrequested direct-chat artifacts, stale deliverables, and known active-skill API misuse before guest execution.
- The local DOCX, PDF, and XLSX skills were compared with public best-in-class patterns and official library contracts before revision. Research-derived references: [Anthropic document skills](https://github.com/anthropics/skills), [openpyxl optimized modes](https://openpyxl.readthedocs.io/en/stable/optimized.html), [python-docx API](https://python-docx.readthedocs.io/en/latest/), [pypdf user guide](https://pypdf.readthedocs.io/en/stable/user/index.html), and [ReportLab user guide](https://docs.reportlab.com/reportlab/userguide/). No new runtime dependency or cloud path was added; shipped behavior remains the pinned local libraries in the no-NIC guest.
- Final physical Apple-silicon default sweep: `small-2026-08-11T10-50-53.815Z.json` returned `small_stress_passed`. All 13 sequential and three concurrent results passed with real pinned `gemma-4-12b-it-qat-q4_0`, real no-NIC guests, immutable traces, exact hidden facts, independently reopened outputs, one context compaction, three repeated context compactions, and `maximumRunning=3`.
- Final focused-only cases also passed on the exact final implementation: PDF report `small-2026-08-11T11-16-25.816Z.json`, Excel report `small-2026-08-11T11-18-55.726Z.json`, and the 24-workbook/31-file multi-format continuation report `small-2026-08-11T11-23-10.727Z.json`. Each produced the requested artifact and passed independent byte-level verification of all four hidden business facts.
- Final canonical `pnpm test:m3:macos` returned `classification: certified`: real Python and Node each completed two executions with one artifact at 131,072 tokens; the no-NIC guest blocked host paths, IPv4/IPv6, DNS, VSOCK, Unix sockets, socket pairs, package managers, credentials, and out-of-scope writes; document probes, cancellation, timeout, output limiting, process/memory/quota limits, crash containment, symlink rejection, persistence, folder revocation, session deletion, and two overlapping VMs passed.
- Final `PATH=/Users/alex/.cargo/bin:$PATH pnpm verify` passed the 300-line source limit, Biome, TypeScript, 112 unit files with 536 tests passed and one declared skip, two native tests, sidecar/resource verification, desktop production build, Cargo formatting/clippy, and macOS/Windows helper build stages. This is physical macOS and local cross-platform build evidence only; no new physical Windows agent, installed-app, or signing result is inferred.

### Plain-language Excel search routing fix - 2026-08-10

- Symptom: the exact user task `Search all excel files in current folder for revenue that came into the business and return the results in a nice table here` never processed the workbooks. On current source the real pinned Gemma agent issued one shell `find /source -type f -iname '*.xlsx'`, then looped rejected duplicates and ended `agent_stalled_duplicate` with no Python, no table, and no progress markers.
- Root cause (production): generic declarative skill routing in `packages/core/src/agent/prompt-library.ts` only activates a format skill when a recognized action verb appears within 120 characters of a format keyword, and the `FORMAT_ACTION` vocabulary omitted `search`/`caută`. The exact prompt therefore left `xlsx-workbooks` inactive, so the model fell back to shell discovery instead of a bounded openpyxl program. The hash-pinned August 4 release bundle (XLSX prompt SHA-256 `a845336fc21d3a80ca61cc6d8a9ae5463ceaf6bba334a6bdc641e78bc81ddc16`, sidecar SHA-256 `1bf22434e3ef8dffa150194788e25bc90a24db2916c855d64e8d3c900bdbd09b`) routed straight to XLSX and did not stall, which localized the regression to current-source routing rather than the fixture.
- Fix (production, four parts): added `search`, `caută`, and `cauta` to the generic `FORMAT_ACTION` verb set; and, in the format-specific `xlsx-workbooks` skill, pinned discovery to the read-only `/source` mount (so "current folder" is not read as `.`), required a separate `print()` per progress marker (adjacent f-strings dropped the newlines), and required each source field to occupy its own full-value table column via a concrete `values_only` row-render idiom (banning row-into-one-cell, fixed-width `[:N]` slicing, and dropped amounts). The skill was kept size-neutral so the certified 8K three-format prompt floor still fits.
- After the fix, `traceError` is null on every physical run: the XLSX skill activates, discovery targets `/source`, execution is Python-first with no shell, and complete `VAULT_PROGRESS_DONE=36`/`TOTAL=36`/`COMPLETE=1` markers print. The shell-first stall regression no longer reproduces. The tracked `excel-chat-path-list` case reached two consecutive clean passes (`small-2026-08-10T14-04-26.087Z.json`, `small-2026-08-10T14-06-17.773Z.json`): all 36 marker/amount pairs on GFM rows, exactly two executions, no artifact.
- Residual limit: the inline 36-row table step still varies run to run at temperature 0 on this local GPU. Later runs showed the model writing its own buggy formatters (dict-indexing a positional row tuple → `list indices must be integers`, empty-pipe tables, or exceeding the execution budget), so the case is classified `small_stress_limit_found` rather than a guaranteed pass. This is the same model-quality variance already documented for `oversized-table-result` and `context-compaction`, not the routing regression, which is fixed. The case's `maxExecutions` was corrected from an unrealistic 2 to 5 (matching the peer `excel-row-filter` case) while every correctness gate (all 36 amounts, XLSX-first routing, `/source` Python discovery, no shell, complete markers, no artifact) stays strict.
- Reconciliation: the ignored `packages/eval/.generated/reproduce-f9dec141.ts` runner, which previously expected the (incorrect) package stall, now verifies the packaged bundle's real correct direct-XLSX behavior (succeeded run, active XLSX first prompt, Python-first, no shell, complete markers, all 36 rows).
- Gates: focused routing/reporting units pass (`prompt-library.test.ts` now asserts the exact task and a Romanian variant route to `xlsx-workbooks`); `PATH=/Users/alex/.cargo/bin:$PATH pnpm verify` passed (498 unit tests, one skip, two native tests, lint, typecheck, source limits, Rust/helper checks, sidecar validation, desktop build); and the canonical physical `pnpm test:m3:macos` returned `classification: certified` (real Python and Node in two executions with artifacts, no-NIC guest isolation, docx/pdf/xlsx probes, limits, persistence, cancellation, two overlapping VMs at 131,072 tokens). macOS evidence only; Windows behavior is not inferred here.

### macOS 128K post-Windows-PR reliability - 2026-08-09

- After the Windows-authored context-compaction PR series merged, `pnpm desktop:dev` failed on macOS because the sidecar bundle resolves `@vault/shared` through its `vault-runtime` export condition (`packages/shared/dist`) but nothing rebuilt that `dist`, so the new `AgentSessionSummarySchema` and `MAX_ANCHORED_SUMMARY_CHARACTERS` exports were missing. `build-sidecar.ts` now compiles `packages/shared` before the core bundle, so a fresh checkout with new shared exports starts without a manual `tsc`. Deleting `packages/shared/dist` and rebuilding reproduces and confirms the fix; `prepare:dev` then bundles Core and the full Tauri desktop starts.
- The physical `pnpm test:stress:m3:small` run on Apple silicon (51 GiB, 131,072-token tier, real pinned Gemma, no-NIC guest) reproduced a deterministic agent stall on `context-compaction`. With temperature 0 the same task prompt reliably produced a Python `IndentationError` on the first attempt; the model then generated a correctly indented repair, but it carried a trailing hallucinated schema-field token (`skills_requested_none`), which the invalid-program guard rejected. Every repair was rejected the same way and the run ended `agent_stalled_duplicate`. Two identical runs failed byte-for-byte (`small-2026-08-09T08-32-34.305Z.json`, `small-2026-08-09T09-05-09.798Z.json`), and the same case passed on this machine on 2026-08-07 (`small-2026-08-07T04-42-36.032Z.json`), so this was a real recovery regression rather than model noise.
- Fix: `normalizeSourceItems` now strips trailing lines composed only of bare identifier tokens (with no punctuation or leading keyword) before delimiter balancing. A repaired program keeps `main()`, `pass`, `return`, real trailing calls, and quoted-string statements, while hallucinated `skills_requested_none` / `command_args_none` debris is removed so a valid repair is accepted. Focused coverage added in `loop-source-recovery.test.ts`; `pnpm test` passed 487 tests with one declared skip, `pnpm test:native` passed, and typecheck, lint, and source limits passed. No model, context, execution, timeout, VM, workspace, memory, or isolation limit changed.
- Re-run after the fix (`small-2026-08-09T09-51-01.386Z.json`): the `context-compaction` stall is gone. The run recovered from the first-attempt crash, one automatic compaction fired, and it returned the exact `COMPACTION_TOTAL=38687`; it now trips only the strict `maxExecutions: 1` harness assertion because the model's first attempt still crashes and needs one repair. `excel-row-filter`, `invalid-document`, `romanian-task`, `no-skill-direct`, `terminal-discovery`, and `repeated-context-compaction` (three compactions, exact stage totals) all passed. `oversized-table-result` remains a separate model-quality limit: Gemma degenerates mid-program into repeated and multilingual junk tokens (`الي`, `இச`, `alfdf…`) inside large source, which is not the clean trailing-debris case, so it still ends `agent_stalled_duplicate`. The concurrent `excel-row-filter` overlap ended `structured_tool_call_required`. The suite classification stays `small_stress_limit_found` on these residual model-quality limits.
- Canonical gate after the fix: physical `pnpm test:m3:macos` returned `certified`. Real Python and Node each completed in two executions with one artifact and two clean attempts (no stall retries), DOCX/PDF/XLSX document probes passed, and guest isolation (host-path, network, IPv6, DNS, vsock, unix, socketpair, package managers, credentials), execution limits, timeout, output truncation (`resource_limit`), process/memory/quota limits, crash handling, symlink rejection, workspace persistence and repair, cancellation, folder revocation, session deletion, and two overlapping VM lifetimes all passed at the 131,072-token tier.
- Anchored long-chat after the fix: physical `pnpm test:stress:m3:context-session` returned `context_session_passed` (`context-session-2026-08-09T10-17-17.137Z.json`). All seven turns succeeded at the 131,072-token allocation, three distinct evolving anchors reached later agent prompts (turns 3–7 `anchored: true`), no required terms were missing, and the final response recovered the exact review style, unresolved item, next move, offline preference, and delivery format. This exercises the anchored session-summary path (`AgentSessionSummarySchema`) whose missing compiled export first surfaced the `pnpm desktop:dev` start failure.

### Dynamic context turnover certification - 2026-08-08

- Prompt fitting now treats the worker's reported allocation as the authority. Focused tests exercise 8,192, 16,384, 65,536, and 131,072 tokens: the same 20,000-character observation compacts at the two smaller allocations and remains verbatim at the two larger allocations, while every serialized request stays inside its allocation after the generation reserve.
- Successful source and commands now share the allocation-derived execution-context budget with stdout and stderr. A completed 128,000-character source compacts and fits at every tested tier; failed repair source remains exact and returns `agent_context_exhausted` when the 8K tier cannot preserve it.
- Older chat now uses its anchored summary as soon as verbatim older messages exceed the current history budget. Summary generation selects the largest pending-message prefix that fits the reported allocation, advances its covered-message cursor by that prefix, and makes one fresh structured-call retry before deterministic fallback.
- The first repeated-execution fixture reached five compacted prompts but failed its final structured call after the model misread lower-case headers (`small-2026-08-08T17-17-18.860Z.json`). Three corrected fixture iterations then exposed target-column, exhausted-reader, and header-case mistakes while still proving three compacted prompts (`small-2026-08-08T17-20-09.664Z.json`, `small-2026-08-08T17-21-44.364Z.json`, and `small-2026-08-08T17-24-34.595Z.json`).
- The exhausted-reader run exposed a product continuity defect: the 24-line anchored ledger retained only the newest execution after generic record lines displaced earlier target rows. Evidence selection now reserves four ranked lines for each of the six possible executions and strongly prioritizes explicit uppercase identifiers and exact labels; an anchored build and a from-scratch build retain the same target rows.
- Final physical Windows repeated-turnover evidence passed on the dynamically reported 65,536-token allocation: exactly three Python executions, exactly three compacted traced prompts, and exact `STAGE_1_TOTAL=116224`, `STAGE_2_TOTAL=218005`, and `STAGE_3_TOTAL=319786`. Report: `small-2026-08-08T17-26-22.017Z.json`.
- The first dynamically sized chat session completed seven real-model runs and recalled all five old decisions, but all summary attempts returned prose rather than the required call, so deterministic excerpts carried continuity and the oracle correctly failed with zero anchors (`context-session-2026-08-08T17-28-30.309Z.json`). A direct service probe identified `structured_tool_call_required`; the new bounded retry then persisted the summary without changing the completed run.
- Final physical Windows chat evidence passed with 69,043-character pressure turns derived from the actual 65,536-token allocation. Seven runs all succeeded, every trace reported that allocation, three distinct evolving anchors reached later agent prompts, and the final response recovered the exact review style, unresolved item, next move, offline preference, and delivery format. Report: `context-session-2026-08-08T17-37-43.754Z.json`.
- The complete final Windows small sweep passed all eight sequential and three concurrent results in 485.8 seconds with `maximumRunning=3`. It repeated the one-compaction exact `COMPACTION_TOTAL=38687` case, the three-compaction exact stage totals, independently reopened `revenue_received_report.xlsx` and `avans_rows.xlsx`, and passed invalid-input, Romanian routing, direct response, terminal discovery, and concurrency checks. Report: `small-2026-08-08T17-41-19.392Z.json`.
- The first post-verify canonical Windows attempt completed every guest stage but could not start packaged inference because verification had cleaned its staged resources. After rebuilding, two identical runs contained the intentional one-million-byte stdout limit correctly but failed finalization with `structured_tool_call_required`. The source-discovery schema had been tested against a nonexistent `output_limit` termination while the typed guest protocol reports exit-zero output truncation as `resource_limit`, so production still removed the response branch and demanded another source action.
- The typed fix allows finalization only for nonempty, exit-zero `resource_limit` evidence; crashes, timeouts, nonzero resource failures, empty discovery, and stderr remain source-recovery-only. The final physical `pnpm test:m3:windows` returned `certified_headless`: the limit run succeeded with exactly 1,000,000 retained stdout bytes and `stdoutTruncated=true`, and Python, Node, cancellation, timeout, process, memory, quota, no-NIC security, malformed-frame teardown, persistence, document probes, and complete teardown passed.
- No model, context, generation, execution, VM, workspace, time, filesystem, network, or isolation limit increased. Real repeated-turnover and long-chat evidence is Windows 64K only; the same allocation-derived runners remain to be executed separately on physical 128K and macOS tiers.

### Anchored context continuity - 2026-08-08

- Two OpenCode-derived improvements landed after comparing its `session/compaction.ts` with ours. OpenCode runs a deterministic `prune()` over old tool output plus a model-written anchored summary that merges into `<previous-summary>`; we previously had only the deterministic half, and no continuity record once a session outgrew its verbatim history budget.
- Deterministic ledger anchoring merges each run's compacted ledger forward instead of recomputing it from every raw stream on every prompt build. Evidence and artifacts are ordered sets and warnings are keyed by execution step, so the merge is exact and adds no drift. A focused test asserts an anchored build and a from-scratch build produce identical ledgers.
- The anchored conversation summary replaces the former `N older messages remain in durable conversation history.` marker, which carried no intent, decisions, or blockers. It is untrusted continuity prose: it never carries `LABEL=value` facts, never gates completion, runs only at or above a 16,384-token context, and falls back to the deterministic marker whenever the model fails, returns nothing usable, or the session is still short.
- Catalog schema v12 stores exactly one replaceable anchor per session, removed with its session by cascade, so a long session keeps one evolving document rather than an unbounded chain.
- Focused coverage passed: anchored merge and compaction equivalence, summary guardrails, history fallback in both directions, catalog migration, and a five-turn service lifecycle proving the anchor is persisted and then merged through `<previous-summary>` on a later turn.
- `pnpm verify` passed after the change: 101 unit files and one declared skip, 476 tests passed with two declared skips, native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar and resource verification, and the desktop production build.
- Physical Windows re-evidence on the final code: focused context compaction passed in one execution with exact `COMPACTION_TOTAL=38687` and one compaction (`small-2026-08-08T15-17-54.959Z.json`); the complete small sweep passed all seven sequential and three concurrent results with `maximumRunning=3`, including independently verified `revenue_received_report.xlsx` and `avans_rows.xlsx` (`small-2026-08-08T15-19-09.777Z.json`); and `pnpm test:m3:windows` returned `certified_headless` with the refreshed guest document probe intact.
- No context, execution, time, VM, workspace, memory, or isolation limit changed. Exact values still come from durable execution records, never from summary prose.

### Windows certification cycle - 2026-08-08

- The canonical physical `pnpm test:m3:windows` gate returned `certified_headless` with the real pinned Gemma worker and x86_64 no-NIC guest. Python, Node, cancellation, timeout, 1,000,000-byte output truncation, malformed-frame teardown, live read-only source, guest workspace persistence, process and memory limits, document-library probes, and host-path and network confinement passed.
- Focused context compaction passed in one execution with exact `COMPACTION_TOTAL=38687`, a 335,045-byte three-file fixture, and one automatic compaction. Report: `small-2026-08-08T09-07-59.351Z.json`.
- The first focused oversized-table run ended as `small_stress_limit_found` after six executions and no artifact. Every generated Python attempt contained a source item at the 512-character structured-output boundary, including repeated zeroes and malformed appended text. Report: `small-2026-08-08T09-13-12.586Z.json`.
- After rejecting boundary-truncated source before guest execution, the identical case reached a clean 4/4 execution and captured `revenue_received_results.xlsx`, but Core then issued contradictory table-repair guidance instead of accepting the overflow workbook and exhausted the planning limit after five executions. Report: `small-2026-08-08T11-43-02.853Z.json`. The final fix also rejects generated entry-point programs that define `main` without invoking it and accepts a captured overflow workbook instead of requiring a duplicate chat table.
- The final identical oversized-table rerun passed in three executions and 120.258 seconds. Core captured `revenue_received_summary.xlsx`, the independent verifier reopened the real artifact bytes, and all 2,000 hidden revenue-row facts were present. Report: `small-2026-08-08T11-49-37.624Z.json`.
- The complete default Windows small sweep passed all seven sequential and three concurrent results with `maximumRunning=3`. Sequential context compaction retained exact `COMPACTION_TOTAL=38687` with one compaction; the oversized result again delivered and independently verified `revenue_received_summary.xlsx`; row filtering, invalid-document clean stop, Romanian routing, direct response, and terminal discovery all passed. Report: `small-2026-08-08T11-52-00.776Z.json`.
- Scaled sequential completed as `scaled_stress_limit_found`. The PDF run produced `scaled-policy-report.pdf` but independent verification found missing `POLICY_PAGES=100`; the 50-workbook, 10-million-row XLSX run used all six executions without a deliverable; the 50-file mixed run stopped after five executions with `agent_stalled_duplicate`. Report: `scaled-sequential-2026-08-08T12-00-22.959Z.json`. These match model and continuation limits rather than a Windows inference fallback: the retained worker evidence records the single-device 17,094,475,776-byte VRAM budget, 9,175,791,872 GPU VRAM bytes, 910,614,240 CPU RAM bytes, and the 65,536-token `windows_gpu_vram_at_most_24_gib` tier. During the run, live `nvidia-smi` samples reached 94 percent GPU compute and 83-84 percent memory-controller utilization with about 10.5 GiB total device allocation including Windows display use.
- The first scaled concurrent Windows run retained the same PDF fact miss and no XLSX deliverable, while the third cold-start cross-format run failed before inference with `agent_context_exhausted`; only two runs overlapped (`maximumRunning=2`). Report: `scaled-concurrent-2026-08-08T12-24-57.305Z.json`. The exact three-skill Gemma request was 4,162 estimated tokens against the certified 4,096-token request floor before the worker reported its 64K allocation. Generic save, reopen, and declaration guidance duplicated across the three active document skills is now compacted without dropping format-specific verification, and the exact scaled task has a floor-budget regression test before the concurrent rerun.
- The corrected scaled concurrent rerun reached `maximumRunning=3`; all three cases entered real inference and guest execution, and the cross-format cold start no longer exhausted context. It remained `scaled_stress_limit_found`: the PDF missed exact `POLICY_PAGES=100`, the XLSX artifact missed exact `MATCHING_INVOICES=500` and `INVOICE_TOTAL=12752750`, and the cross-format run completed six executions without a deliverable. Report: `scaled-concurrent-2026-08-08T12-33-15.858Z.json`. This closes the prompt-budget and scheduler regression while retaining the separate model-quality limits accurately.

### Three-skill prompt budget after merging generated-file freshness - 2026-08-07

- Merging the generated-file freshness work into this branch made a three-format request exceed the 8K prompt floor. `Create report.pdf, report.docx, and report.xlsx.` activates the DOCX, PDF, and XLSX skills together, and the merged request serialized to 4,104 tokens against the fixed 4,096-token request budget, so `generationInput` returned `agent_context_exhausted` before any inference ran. The same request fit at 14,940 prompt characters before the merge.
- Both branches were individually correct. The freshness change added the same deliverable-recreation sentence to all three document skills, and this branch added compacted task state and deliverable-fact instructions, so only the combination crossed the limit.
- The duplicated sentence is generic rather than format-specific, so it now appears once in the always-loaded system prompt instead of three times in skill bodies. Behavior is preserved for every deliverable format, including formats without a document skill, and the three-skill request fits again. No context, execution, time, VM, workspace, or isolation limit changed.


The 2026-08-06 context-turnover regression cycle split the small profile's stochastic management-report requests into directly runnable focused PDF, DOCX, XLSX, and large-corpus cases. The redundant monolithic request repeatedly kept Gemma in one constrained generation until the fixed worker timeout, while each format-specific case exercised the same hidden four-fact corpus and passed direct artifact-byte verification. The default sequential and concurrent sweeps use generic deterministic cases so a worker-generation timeout cannot prevent agent regressions from running. The scaled profile retains cross-format and large-corpus continuation workflows; no product context, inference, execution, VM, workspace, time, or isolation limit changed.

### Unbacked promise-only response recovery — 2026-08-07

- A reported local session finished as `succeeded` for `Write a short story for children in word & pdf docs` with zero executions, zero artifacts, an empty workspace manifest, and only the promise `I will create a short story for children titled 'The Brave Little Squirrel' and save it as both a DOCX and a PDF document in the /workspace directory.` The supplied debug snapshot preserved both traced turns, the exact prompts and schemas, and outcomes `accepted_skill_request` and `accepted_response`, which was sufficient to diagnose the defect without a new capture.
- Root cause was three compounding gaps: the loop accepted any `respond` decision before checking that requested deliverables existed, `requiresSourceExecution` left the `respond` branch in the decision schema because no active skill required execution evidence, and only `xlsx-workbooks` declared progress markers so DOCX and PDF deliverables had no execution-backed completion requirement. Abbreviated `word & pdf docs` phrasing also failed DOCX keyword routing, spending the entire first turn on a skill-activation round trip that emitted a duplicate `Loading the local model and planning the task.` event.
- After the fix, the identical task ran twice through the real current-user daemon, pinned Gemma worker, and no-NIC guest and succeeded both times. Traced outcomes were `rejected_unbacked_response`, `accepted_execution`, `accepted_execution`, `accepted_response`. The run persisted `Pip_and_the_Magical_Acorn.docx` at 36,974 bytes and `Pip_and_the_Magical_Acorn.pdf` at 2,089 bytes. The first guest attempt exited 1 with `crash` and the model repaired it in one further execution, so recovery remained real rather than assumed. This is macOS evidence only.
- The refused turn is now recorded rather than silently accepted: catalog schema version 11 adds the typed `rejected_unbacked_response` outcome. The first real rerun failed closed with `agent_model_failed` until that migration existed, which confirmed the constraint path rather than masking it.
- Deliverable policy stays in skill metadata through the new `produces-deliverables` flag, so `packages/core/src/agent/deliverable-completion.ts` names no document format and the generic agent architecture boundary test still passes.
- Focused coverage passed: 166 tests across 29 agent files, including a promise-only rejection, a clarifying-question rejection, schema removal of the `respond` branch on the retry turn, abbreviated Word routing, and single-emission planning messages. `pnpm verify` passed 95 unit files with 425 tests and one declared skip, two native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar validation, and the desktop production build. The canonical physical `pnpm test:m3:macos` gate returned `certified` both before and after the migration, with the 131,072-token context tier and two overlapping VM lifetimes.

### Generated-file freshness and planning recovery — 2026-08-07

- The exact reported task `Write a short story for children in a word and a pdf doc` passed through the real current-user daemon, pinned Gemma worker, and no-NIC guest. It persisted exactly `story.docx` and `story.pdf`; signatures were `504b0304` and `25504446`; an independent guest reopened both as non-empty documents; final declarations matched both artifact rows; materialized and exported bytes matched; and the 25-event audit chain verified. Report: `generated-deliverables-2026-08-07T08:00:07.619Z.json`.
- The richer hidden-fact two-file fixture also passed in two executions and three traced turns. Both artifacts contained all four hidden facts, both signatures and export bytes verified, and the 15-event audit chain passed. Report: `generated-deliverables-2026-08-07T07:44:08.088Z.json`.
- The focused XLSX row-filter oracle passed in two executions and 96.209 seconds. Independent reopening verified `avans_rows.xlsx` with all hidden matching rows and no ordinary-transfer rows. Report: `small-2026-08-07T08-00-29.416Z.json`.
- The first focused PDF report run created `management-report.pdf`, but independent byte verification correctly failed because the file contained `INVOICES=0` and `TOTAL=0.0` instead of the hidden invoice facts; `NOTES=24` and `PAGES=12` were correct. Report: `small-2026-08-07T08-02-17.869Z.json`. This exposed case-sensitive workbook-header matching; the workbook skill now requires case-insensitive normalized headers and fail-closed missing-column handling.
- The identical PDF report rerun extracted the exact hidden facts `MATCHING_INVOICES=8`, `INVOICE_TOTAL=20012.0`, `MEETING_NOTES=24`, and `POLICY_PAGES=12`, created and reopened the PDF, but Gemma failed to issue a final structured declaration after five executions. Core failed closed with `structured_tool_call_required` and persisted no artifact. Report: `small-2026-08-07T08-06-51.776Z.json`. This remains a real-model quality limit; no execution, time, context, security, or planning limit changed.
- The canonical physical `pnpm test:m3:macos` gate returned `certified` with real Python and Node artifacts, persistence, cancellation, resource limits, no-NIC confinement, two overlapping VM lifetimes, and the 131,072-token context tier. `pnpm verify` passed 94 unit files with 415 tests passed and one declared skip, two native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar/resource validation, and the desktop production build.
- Core now keeps captured deliverables until an authoritative workspace delta changes or removes their paths, rejects bare interactive Python or Node shell commands before VM execution, keeps shell misuse recovery source-only until one source execution succeeds, and labels planning retries as revised plans. stdout, stderr, exit status, and unrelated programs do not change artifact freshness. This is macOS evidence only.

### Context turnover and oversized result cycle — 2026-08-07

- Before the fix, the real Gemma/no-NIC context case found the hidden exact total but failed `Expected automatic context compaction evidence.` with `contextCompactions=0`; its 406,996-byte stdout was only head/tail elided. The oversized table case consumed all six executions, claimed `/workspace/revenue_results.txt`, declared no artifact, and verified zero rows. Reports: `small-2026-08-06T15-36-20.066Z.json` and `small-2026-08-06T15-38-58.605Z.json`.
- Final focused context compaction passed in one execution with exact `COMPACTION_TOTAL=38687`, a 394,996-byte durable observation, and one traced inspectable compaction ledger. Report: `small-2026-08-07T04-14-13.705Z.json`.
- Final focused oversized result delivery passed in six executions. Gemma chose `revenue_received_summary.xlsx`; deterministic reopening with the pinned offline workbook libraries verified all 2,000 hidden `REVENUE_ROW_0001` through `REVENUE_ROW_2000` facts from real artifact bytes. Report: `small-2026-08-07T04-17-23.324Z.json`.
- The final default small sweep passed all seven sequential and three concurrent results with `maximumRunning=3`. It repeated both new cases successfully, plus XLSX row filtering, invalid-PDF clean stop, Romanian workbook routing, direct response, and terminal evidence translation. Report: `small-2026-08-07T04-42-36.032Z.json`.
- Focused management reports remained directly runnable and passed deterministic byte verification against the same four hidden facts: PDF in three executions (`small-2026-08-06T20-10-45.660Z.json`), DOCX in three (`small-2026-08-06T17-51-38.950Z.json`), XLSX in four (`small-2026-08-06T17-59-13.017Z.json`), and large-corpus XLSX in four (`small-2026-08-06T18-01-37.189Z.json`).
- Corrected scaled sequential completed as `scaled_stress_limit_found`: PDF produced `scaled-policy-report.pdf` but missed hidden `POLICY_PAGES=100`; 50-workbook XLSX completed six executions without a deliverable; 50-file cross-format stopped with `agent_stalled_duplicate`. Report: `scaled-sequential-2026-08-07T06-03-47.586Z.json`. The earlier completed concurrent run retained `maximumRunning=3` but all three report cases failed artifact verification (`scaled-concurrent-2026-08-07T05-31-03.264Z.json`). A corrected concurrent rerun was stopped after repeated model stalls and an `IO_ENOSPC` execution; it produced no qualifying report.
- The canonical physical `pnpm test:m3:macos` gate returned `certified` with real Python and Node artifacts, persistence, cancellation, resource limits, no-NIC confinement, two overlapping VM lifetimes, and the 131,072-token context tier. `pnpm verify` passed 94 unit files with 424 tests passed and one declared skip, two native tests, TypeScript, lint, source limits, Rust and native helper checks, sidecar/resource validation, and the desktop production build.

Focused XLSX row-filter regression validation on 2026-08-06 used the pinned Gemma worker, current-user daemon, and real no-NIC guest on physical Apple silicon. The fixture contained exactly 10 uppercase-extension XLSX workbooks under 10 subfolders, each with exactly 100 rows including the header. One hidden row per workbook contained `avans`; expected row markers remained outside the model prompt. The user task was the original terse request: `Generate an excel with all rows in excel files containing “avans”`.

- Before the fix, the agent reproduced the original sequence: unnecessary import failure, incorrect workbook-level dimension recovery, nested row serialization failures, repeated repairs, and all six executions consumed. The deterministic artifact check confirmed that the final workbook eventually contained all 10 records and no nonmatching rows, but the case correctly failed with `Expected at most 5 executions.` Report: `small-2026-08-06T08-50-49.525Z.json`.
- After adding explicit worksheet-dimension, flat-scalar row serialization, and malformed-Python recovery guidance to the XLSX skill, the identical case passed in two executions and 93.181 seconds. `avans_rows.xlsx` contained all 10 hidden records, no nonmatching `ordinary transfer` row, and the terminal execution reported clean `10/10` complete progress. Report: `small-2026-08-06T08-48-41.857Z.json`.
- No runtime execution, time, memory, context, or isolation limit changed. The focused stress oracle requires completion within five of the six available executions so capacity-exhaustion regressions fail. Focused prompt, loop, stress-profile, reporting, and TypeScript checks passed before the final physical run. This is macOS evidence only.

Focused generic-agent regression validation on 2026-08-06 ran on physical Apple silicon with the pinned Gemma worker and real no-NIC guest. The canonical `pnpm test:m3:macos` gate returned `certified`: real Python and Node tasks each completed with two executions and one artifact, while guest rehydration, repair, persistence, cancellation, timeout, output, security, workspace lifecycle, and two overlapping VMs passed at the 131,072-token hardware tier.

| Focused case | Final result | Exact evidence | Retained ignored reports and intermediate limits |
|---|---|---|---|
| Invalid document | **PASS** | One Python execution; real `EOF marker not found` evidence; exit 0; no artifact or retry. | The first run correctly identified the invalid PDF but used a separate discovery execution and failed `Expected at most 1 executions.` (`small-2026-08-05T21-53-47.305Z.json`). The corrected identical case passed in `small-2026-08-05T21-56-05.895Z.json`. |
| Romanian workbook routing | **PASS** | Unicode routing activated the workbook skill; complete clean progress; exact hidden `TOTAL_AVANS=12009`; three executions. | The first two runs found the exact total but ended `structured_tool_call_required` after concatenated markers or non-error stderr (`small-2026-08-05T21-56-56.638Z.json`, `small-2026-08-05T22-01-50.563Z.json`). A clean-evidence run then exposed ambiguous fixture column wording and returned `TOTAL_AVANS=0.0` (`small-2026-08-05T22-03-49.820Z.json`). After requiring separated markers, empty successful stderr, and explicit input column names, the identical case passed in `small-2026-08-05T22-05-56.358Z.json`. |

These runs validate only the canonical macOS gate and the two named focused stress cases. The complete small suite, scaled suite, and Windows certification were not run against this refactor.

The generic-agent refactor validation on 2026-08-05 used the new artifact-first `pdf-report` case on physical Apple silicon with the pinned Gemma worker and real no-NIC guest. The evaluator generated 11 realistic mixed-format files, kept expected facts outside the prompts, materialized the declared PDF, and opened it in a separate verification session. The production run derived the correct current facts (`MATCHING_INVOICES=8`, `INVOICE_TOTAL=20012.00`, `MEETING_NOTES=24`, `POLICY_PAGES=12`) and declared `management-report.pdf`, but independent extraction found unrelated fabricated metrics such as `REVENUE=450M`, `NET_INCOME=140M`, and `EMPLOYEE_COUNT=1200` in the artifact. The case therefore **failed** as `small_stress_limit_found`; response text and a declared artifact did not override artifact-byte evidence. The retained ignored report is `packages/eval/.generated/stress/small-2026-08-05T20-39-06.161Z.json`.

This physical run also exposed and drove generic fixes before the retained result: format-neutral progress could no longer auto-finish a multi-format deliverable task, caught workbook errors could not prove complete progress, and the then-current heuristic treated a deliverable as stale after later calculations unless recreated or explicitly verified. The 2026-08-07 delta-authoritative freshness change above supersedes that heuristic. The final retained failure is model output quality, not a passing product claim. The complete small suite, scaled suite, and Windows certification remain required before certification; the canonical macOS gate was subsequently rerun as recorded above.

The latest validation cycle was performed on 2026-08-03 on physical Apple silicon against `m3-stress-reliability` after the three reliability changes. This is macOS evidence only; it does not establish current Windows behavior.

The realistic fixture contained 10 files: TypeScript, JSON, CSV, Markdown, two XLSX workbooks with 10,000 rows, two DOCX files with 16 pages, and an eight-page PDF. Five sessions were started together.

| Suite or check | Result | Evidence | Failure or limit |
|---|---|---|---|
| Small stress suite | **PASS** | All 8 sequential and concurrent results passed twice consecutively. | None. |
| Sequential PDF | **PASS** | `PDF_PAGES=12`; `PDF_CHECKSUM=1326`. | None. |
| Sequential workbook | **PASS** | `XLSX_MATCHES=2`; `XLSX_TOTAL=2003`. | None. |
| Sequential XLSX folder | **PASS** | `XLSX_MATCHES=6`; `XLSX_TOTAL=12009`; one execution in both final runs. | None. |
| Sequential mixed folder | **PASS** | Exact XLSX and DOCX counts and checksums. | None. |
| Invalid PDF | **PASS** | Printed `INVALID_DOCUMENT_STOP=1` in one successful execution with no repair or artifact. | None. |
| Three concurrent cases | **PASS** | PDF, XLSX-folder, and mixed-folder cases passed with `maximumRunning: 3`. | None. |
| Realistic skill suite | **PASS** | All 5 cases passed twice consecutively across five simultaneously started sessions; audit valid. | None. |
| Direct response; no skill | **PASS** | Returned a correct concise response with zero executions. | None. |
| Terminal-only source discovery | **PASS** | Located `/source/src/pricing-rules.ts` and returned `SURCHARGE_BPS=275` without guessed paths or duplicate stall. | None. |
| Hidden-answer XLSX analysis | **PASS** | `XLSX_MATCHES=4`; `XLSX_TOTAL=6006.0`. | None. |
| Attached-PDF analysis | **PASS** | `PDF_PAGES=8`; `PDF_CHECKSUM=612`. | None. |
| Combined terminal and XLSX | **PASS** | Returned exact source evidence, `XLSX_MATCHES=4`, `XLSX_TOTAL=6006`, and `q2-audit.md` in both final runs. | None. |
| Dynamic skill disclosure | **PASS** | Direct: none; terminal: `terminal-commands`; workbook: `xlsx-workbooks`; PDF: `pdf-reading`; combined: terminal plus XLSX. Unselected skill bodies remained unloaded. | None. |
| Model and audit | **PASS** | Audit chain verified; model moved from `unloaded` to `ready`. | None. |
| Scaled sequential | **PASS** | 100-page PDF, one-million-row workbook, 50 workbooks, and 50-file mixed workflow returned exact results. | Large workbook workflows required resumable recovery and exceeded the intended 75-second work window in individual executions. |
| Scaled concurrent | **PASS** | Workbook, 50-workbook, and mixed workflows passed with `maximumRunning: 3`. | Large workflows used four to six executions and long-running batches. |
| Canonical `pnpm test:m3:macos` gate | **CERTIFIED** | Real Python and Node artifacts, persistence, cancellation, limits, live read-only source, confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token cap passed. | macOS evidence only. |
| `pnpm verify` | **PASS** | Source limits, lint, type checking, 335 unit tests with one skip, two native tests, native helpers, desktop builds, and Rust checks passed. | None. |

The backend, daemon, model, guest isolation, audit, and canonical gate remained healthy. All four original reliability failures now pass. The final scaled suites also passed, while their long resumable executions remain reported separately rather than treated as proof of tighter work-window behavior.

## Reliability fix validation

### Clarifying-question tool cycle — 2026-08-12

- Physical Windows/NVIDIA continuation reproduced two provider-boundary failures after the final flat question adapter. The canonical headless gate first failed its Python case twice with `agent_turn_limit_exceeded`: a max-token turn switched the loop to response-only mode, raw function-call markup was then rejected while typed tools stayed disabled, and 40 inference turns completed with zero guest executions. The generic retry now restores typed tools whenever raw protocol text is rejected; no model, context, execution, timeout, or VM limit changed.
- The next canonical run passed the Python phase and reached Node, where protocol-control transitions appeared inside a formally parsed `bash.command`. Core executed the corrupted command twice, recovered to a clean command and successful `node-start`/`node-finish` execution, then failed an empty final model turn as `agent_empty_response`. The final generic boundary rejects a nested protocol transition as a failed result for only that call, keeps valid sibling calls, and permits only one empty-answer recovery attempt against retained evidence.
- Focused regression tests cover both exact state sequences. The ignored real pinned-Gemma/current-user-daemon/no-NIC reports `question-windows-canonical-2026-08-12T19-41-21.958Z.json` and `question-windows-canonical-2026-08-12T19-50-39.655Z.json` passed the Python task, with the latter also running the complete Windows guest certification phase. The post-fix exact Node report `question-windows-node-2026-08-12T20-04-27.994Z.json` passed after the same guest phase. The reports retain terminal snapshots, ordered events, complete executions, version-1 inference traces, and valid audit chains.
- A later canonical cancellation case exposed an indirect Python wrapper that used `subprocess.run(..., capture_output=True)`, hiding `cancel-start` until its 60-second child exited and preventing live cancellation. A temporary primary-prompt and tool-description rule required direct Python and Node execution. The ignored exact pre-fix report `question-windows-cancel-2026-08-12T20-19-53.929Z.json` retained `cancelRequested: false` and a succeeded 60-second execution; the identical post-guidance report `question-windows-cancel-2026-08-12T20-26-21.471Z.json` retained `cancelRequested: true`, a live `cancel-start`, cancelled execution and run states, and a valid audit chain. The later general-architecture review removed this task-specific prompt rule because it could over-fit model behavior. Cancellation remains a typed execution and process-control boundary.
- The next full canonical rerun passed Python, Node, and cancellation, then failed only `Windows stdout truncation proof failed`: the output-limit case reached terminal without the selected evidence execution reporting `stdoutTruncated`. This non-qualifying run is retained in the command transcript while an exact ignored daemon/CLI fixture isolates its executions and trace before any further edit.
- The exact isolated output report `question-windows-limit-2026-08-12T20-44-00.022Z.json` retained three `resource_limit` executions with exactly 1,000,000 stdout bytes and `stdoutTruncated: true`. The product boundary passed; the canonical helper had selected an earlier marker-only execution. Its evidence selector now requires a truncated execution for the truncation case and searches all executions for the finish marker. The complete physical Windows gate then returned `certified_headless` on 2026-08-13 in 678.8 seconds: Python and Node succeeded, live cancellation ended both run and execution as cancelled, the selected output execution retained exactly 1,000,000 bytes with truncation, guest document libraries and confinement passed, every VM retained teardown, and malformed-frame HCS teardown passed.
- The general-architecture rerun on 2026-08-13 removed the temporary direct Python and Node prompt rule and reached the cancellation proof after the Python, Node, and guest stages. Gemma staged the requested source with shell, then ran it through a Python `subprocess.run(..., capture_output=True)` wrapper. The run succeeded after 60 seconds because the gate waited for hidden `cancel-start` output before it sent Stop. This is a cancellation-proof coupling failure, not a Core cancellation failure. The gate now sends Stop when any run execution is active, which matches the desktop Stop lifecycle and does not require a model-specific execution form. The output-truncation selector remains unchanged.
- The first proof-only adjustment sent Stop while the Windows VM was still starting. The run changed to cancelled, but this did not prove cancellation of an active guest process and no teardown diagnostic was attached to that execution. The gate now waits for the existing guest `process_start` diagnostic before it sends Stop. It does not wait for program stdout, and it does not change Core or worker behavior.
- The next physical `pnpm test:m3:windows` run returned `certified_headless` in 518.4 seconds. Python and Node succeeded with `process_start`, `process_exit`, and retained teardown evidence. Stop cancelled an active guest process without waiting for stdout; the cancelled execution retained zero stdout bytes and complete process and teardown diagnostics. The output-limit execution retained exactly 1,000,000 stdout bytes with `stdoutTruncated: true`. Guest document libraries, persistence, repair, cancellation, timeout, output, process, memory, quota, crash, symlink, host-path, credential, and network confinement checks passed, and malformed-frame HCS teardown passed.
- The following physical `pnpm test:stress:m3:small` run returned `small_stress_limit_found` after 2,080.8 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T07-40-23.340Z.json`. Six of 16 sequential and concurrent results passed. PDF merge, Romanian analysis, direct no-skill response, terminal discovery, concurrent XLSX edit, and concurrent PDF merge passed. Sequential and concurrent DOCX and sequential XLSX edits produced and independently verified their requested files but exceeded strict execution counts after repeated Gemma syntax errors. Excel row filtering produced an XLSX that failed deterministic content verification. The path-list response contained the 36 values, but it missed required progress tokens and its first trace did not contain active XLSX guidance. Invalid-document and repeated-compaction cases exceeded execution limits. The oversized-table case used 18 executions and remained a known model-quality limit.
- The same small report retained complete terminal snapshots, events, executions, artifact metadata, and version-1 inference traces. It contained no corrupt-argument failure result, unknown-tool result, or `agent_empty_response`. Two cases, `word-report` and `context-compaction`, ended as `inference_failed` before a model response or guest execution. Two raw-response rejections occurred only in `oversized-table-result` and recovered to later tool calls. The report does not retain audit rows or materialized deliverable bytes; the harness verified deliverables before cleanup and then removed its temporary workspace. Audit-chain inspection and independent post-run visual opening were therefore not possible from this report. Per the stop rule, no context-session or scaled suite was started.
- `pnpm test:m3:windows:memory` passed in 16.6 seconds with the packaged CUDA/AppContainer worker. The RTX 5070 Ti exposed exactly 17,094,475,776 dedicated VRAM bytes, selected the 65,536-token `windows_gpu_vram_at_most_24_gib` tier, measured 9,175,791,872 GPU VRAM bytes and 910,614,240 CPU RAM bytes, and shut down cleanly.
- The general-agent change kept the physical Windows gate green. `pnpm test:m3:windows` returned `certified_headless` in 622.5 seconds. Python, Node, active-process cancellation, the 1,000,000-byte output limit, DOCX/PDF/XLSX libraries, workspace persistence and repair, process, memory, quota, crash and symlink controls, guest isolation, and complete VM and HCS teardown passed.
- The first focused real-model `word-report` run after the general-agent change returned `small_stress_limit_found` in 84.3 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-12-42.568Z.json`. The run succeeded with four clean executions, no model failure, a valid audit chain, and a retained `management-report.docx`. Independent extraction found all four source facts in the response but not as complete required label/value pairs in the document. The final program also copied values from earlier tool output instead of reading source files. This is a DOCX workflow failure, not a Core retry or tool-boundary failure. The retained artifact is under `packages/eval/.generated/stress/artifacts/bc73a0e8-829e-4a28-b2b8-692b7d137925/`. LibreOffice was not available in the bundled Windows document runtime, so PNG render inspection was not possible; structural extraction remained the acceptance authority for this failed run.
- The identical focused `word-report` rerun after the prose-level DOCX guidance returned `small_stress_limit_found` in 98.3 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-16-04.547Z.json`. The run succeeded with seven executions, one failed program, no model failure, a valid audit chain, and a retained `management-report.docx`, but it exceeded the six-execution limit. Independent extraction again found no complete required label/value pairs. The prose rule did not change the document shape. The next change is a small reusable DOCX program pattern, not Core logic or another general prompt rule. The retained artifact is under `packages/eval/.generated/stress/artifacts/70ffb2e8-9ffc-4021-b523-8fd7dd8b54e1/`.
- The focused `word-report` run with the small DOCX program pattern returned `small_stress_limit_found` in 86.7 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-20-44.171Z.json`. The run succeeded with two executions, one failed program, no model failure, a valid audit chain, and a retained `management-report.docx`. Independent extraction passed the invoice total, meeting count, and policy page checks. It failed only the complete matching-invoice count pair. The pattern reduced the work from seven executions to two, but the remaining document structure still needs inspection. The retained artifact is under `packages/eval/.generated/stress/artifacts/3fa5df53-fbdb-4c69-b49d-0454b9a15d69/`.
- The next identical focused `word-report` run returned `small_stress_limit_found` in 103.0 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-23-10.807Z.json`. It used eight executions with four failed programs, exceeded the six-execution limit, and independently failed all four exact fact checks in the retained `management-report.docx`. The run had no model failure and retained a valid audit chain. This regression after a near-pass shows model-run variance. No additional prompt rule is justified until the failed programs are inspected. The retained artifact is under `packages/eval/.generated/stress/artifacts/09c6aa3d-641b-4905-ae9f-19b1d80bf359/`.
- The focused `context-compaction` run on 2026-08-13 proved the new bounded inference recovery. The first inference failed, the loop retried the local model once, and the second inference failed. The run then stopped with two inference failures, no guest execution, no compaction, and no unbounded retry. The worker state was `unloaded` after the second failure. The stress harness then threw during its final model-state check and did not write its JSON report. A test-only evidence fix now keeps the completed case result and records the final model-state error in the report.
- The identical focused `context-compaction` rerun after the test-only evidence fix returned `small_stress_limit_found` in 34.7 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-31-18.194Z.json`. The first inference failed, the loop made its one retry, and the second inference failed. The run then stopped with two inference failures, no guest execution, no compaction, and a valid audit chain. The report retained `modelAfterError` with the final `unloaded` model state. This proves both the bounded product recovery and the complete failed-run evidence path.
- The focused `word-report` run after the test task defined each label's value returned `small_stress_passed` in 78.5 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-36-21.649Z.json`. It used four clean executions, had no inference failure, created one retained `management-report.docx`, and passed independent deterministic extraction with a valid audit chain. The verifier accepts a visible label with its nearby correct value and no longer requires one fixed punctuation style. The retained artifact is under `packages/eval/.generated/stress/artifacts/248c814d-cbf1-4f2f-950c-ebdcc67c58a5/`.
- The focused `docx-edit` run returned `small_stress_limit_found` in 89.8 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-38-27.295Z.json`. It created and independently verified the retained `revised-risk-brief.docx`, had no inference failure, and kept a valid audit chain. It failed the stress budget because it used five executions with three failed programs. The retained artifact is under `packages/eval/.generated/stress/artifacts/63d2985f-cb67-4225-8755-fc33b8cf44c9/`.
- The focused `xlsx-edit` run returned `small_stress_passed` in 60.7 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-41-03.821Z.json`. It used two clean executions, had no inference failure, created one retained `revised-budget.xlsx`, and passed independent deterministic workbook verification with a valid audit chain. The retained artifact is under `packages/eval/.generated/stress/artifacts/5ee49413-4cc9-43b6-86c3-a2d4491342ad/`.
- The focused `excel-row-filter` run returned `small_stress_passed` in 70.7 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-42-24.700Z.json`. It used three clean executions, had no inference failure, created one retained filtered workbook, and passed independent deterministic verification with a valid audit chain. The retained artifact is under `packages/eval/.generated/stress/artifacts/5764be48-fb77-43bb-b408-ce38452b93a8/`.
- The focused `excel-chat-path-list` run returned `small_stress_limit_found` in 119.4 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-44-50.829Z.json`. It used five executions with three failed programs, exceeded the three-execution budget, returned no complete 36-row result, and created no normal deliverable. It had no inference failure and kept a valid audit chain.
- The next `excel-chat-path-list` attempt was interrupted by the command host after five minutes and wrote no JSON report. Its read-only catalog showed no guest execution, two automatic context compactions, and a later model turn still marked as running when the process tree ended. This attempt is diagnostic evidence only. It is not a stress pass or fail.
- The focused `invalid-document` run returned `small_stress_passed` in 53.2 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-57-14.986Z.json`. It used one clean execution, reported `EOF marker not found`, created no artifact, had no inference failure, and kept a valid audit chain.
- The focused `repeated-context-compaction` run returned `small_stress_limit_found` in 146.5 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T12-59-01.104Z.json`. It returned all three exact stage totals, had seven clean executions, no inference failure, no artifact, and a valid audit chain. It failed the four-execution limit and recorded no automatic context compaction. The execution and spill evidence needs inspection before any product change.
- The repeated large-output continuity run after the result-first evaluator and spill-search rule returned `small_stress_limit_found` in 93.1 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T13-08-05.215Z.json`. It stayed within four executions, had one failed execution, no inference failure, no artifact, and a valid audit chain. The final answer omitted all three required stage labels, so hidden tool output did not make the wrong user-visible result pass.
- The identical repeated large-output run after the raw-terminator boundary fix returned `small_stress_limit_found` in 130.1 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T13-10-52.350Z.json`. Its final answer contained all three correct stage labels. It used six executions with two failed executions, had one inference failure, created no artifact, and kept a valid audit chain. It failed only the four-execution budget. The raw terminator no longer became the final user answer.
- The next repeated large-output run after aligning the repair budget returned `small_stress_limit_found` in 227.6 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T13-17-03.555Z.json`. It used 15 executions with two failed executions, had no inference failure, created no artifact, and kept a valid audit chain. Its final answer omitted all three required stage labels. The six-execution guard correctly rejected this run. No further budget increase is justified.
- The repeated large-output run with the head-and-tail tool-output preview returned `small_stress_limit_found` in 139.5 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T13-22-50.813Z.json`. It used eight clean executions, had no inference failure, created no artifact, and kept a valid audit chain. Its final answer again omitted all three required stage labels and exceeded the six-execution guard. The preview change alone did not fix the model's stop failure.
- The focused `excel-chat-path-list` run after the head-and-tail preview returned `small_stress_limit_found` in 150.7 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T13-29-39.109Z.json`. It used eight executions with six failed programs, had no inference failure, created no artifact, and kept a valid audit chain. Its final answer contained none of the 36 required traceable marker and amount pairs. The six-execution and three-failure guards correctly rejected the run.
- The first `oversized-table-result` attempt after the result-contract cleanup was interrupted by the command host after six minutes and wrote no JSON report. No test process remained after the timeout. This attempt is diagnostic evidence only. It is not a stress pass or fail.
- The identical `oversized-table-result` attempt with a 15-minute command-host limit was also interrupted and wrote no JSON report. The live catalog reached ten executions with three failed programs and a current model turn before the process ended. The catalog remained marked `running` because the command host ended outside the product lifecycle. This attempt is diagnostic evidence only. It is not a stress pass or fail.
- The final packaged physical Windows gate returned `certified_headless` in 611.1 seconds after the Core, prompt, skill, and stress-contract changes. Python, Node, active-process cancellation, the 1,000,000-byte output limit, DOCX/PDF/XLSX libraries, workspace persistence and repair, process, memory, quota, crash and symlink controls, guest isolation, and complete VM and HCS teardown passed.
- The focused `excel-chat-path-list` run with the user-result contract returned `small_stress_passed` in 126.5 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T14-18-23.895Z.json`. The user-visible table contained all 36 expected revenue amounts, created no artifact, had no inference failure, and kept a valid audit chain. The report also records seven executions and five failed programs as model-quality evidence without letting those private counts override a complete user result.
- The focused `oversized-table-result` run returned `small_stress_limit_found` in 113.3 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T14-21-16.238Z.json`. It used six executions with three failed programs, had no inference failure, created `revenue_results.json`, and kept a valid audit chain. The verifier expected an XLSX workbook, but the task did not name an output format. This is a hidden test requirement that needs correction before any product change.
- The identical `oversized-table-result` run after the task named its required XLSX deliverable returned `small_stress_passed` in 75.9 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T14-24-43.293Z.json`. It used three executions with one failed program, had no inference failure, created one retained `revenue_received_report.xlsx`, and passed independent deterministic verification of all 2,000 expected row identifiers with a valid audit chain. The retained artifact is under `packages/eval/.generated/stress/artifacts/ca8e90f3-935c-43a7-bb60-bc0e0c060fc3/`.
- Independent structural inspection of that retained workbook found one sheet, 2,000 complete data rows, six complete columns, the correct first and last row identifiers, and no formula errors. The top and end visual renders showed that long header and data text was cut off by narrow default column widths.
- The first focused `oversized-table-result` run after the column-width skill rule was interrupted when the command host stopped waiting after 124 seconds. The real stress process ended shortly after that limit, but it wrote no JSON report or retained artifact. This attempt is diagnostic evidence only. It is not a stress pass or fail.
- The identical focused `oversized-table-result` run with a longer command-host limit returned `small_stress_passed` in 264.7 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T14-34-44.510Z.json`. It created one retained `revenue_received_summary.xlsx`, passed independent verification of all 2,000 expected row identifiers, had no inference failure, and kept a valid audit chain. The report records seven executions with four failed programs as model-quality evidence. The retained artifact is under `packages/eval/.generated/stress/artifacts/dfc6d570-5fe8-4490-88e0-1b28087405e2/`.
- Independent structural and visual inspection of that workbook again found the complete 2,000-row data contract and no formula errors. The XLSX had no saved column-width block and no frozen pane, so the long visible text was still cut off. The run trace also shows three repeated Python syntax failures from a leaked final brace before a shorter corrected program succeeded. No source-rewrite or language-specific Core recovery was added.
- The focused `oversized-table-result` run after the XLSX skill gained that short implementation pattern returned `small_stress_passed` in 81.0 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T14-41-30.831Z.json`. It used three executions with one failed program, had no inference failure, created one retained `revenue_received_report.xlsx`, and passed independent verification of all 2,000 expected row identifiers with a valid audit chain. The retained artifact is under `packages/eval/.generated/stress/artifacts/00d3a8b9-ce86-46c1-a709-9439ff965d23/`.
- Independent structural and visual inspection of that latest retained workbook passed the one-sheet, 2,000-row, six-column, first-row, last-row, and formula-error checks. The workbook still had no saved column widths and no frozen pane, and long visible text was cut off in both the top and end renders. The trial width rule did not change the model output and was removed. Core did not post-process the artifact because workbook presentation policy does not belong in the generic session loop.
- The attempted complete legacy small sweep on 2026-08-13 was stopped by the owner after it ran for more than 20 minutes and reached the three concurrent file cases. It wrote no JSON report and is not a pass or fail. The owner set the V1 rule that stress acceptance must fit the certified model size and must not become a model-capability contest. The default small gate now contains only source inspection, XLSX edit, DOCX edit, invalid-document stop, policy rejection, and audit verification. Large-corpus, context-session, and concurrent model workloads remain optional diagnostics.
- The first four-case V1 gate finished in 226.7 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T15-22-20.870Z.json`. Source inspection, XLSX edit, and DOCX edit passed. The invalid PDF used one clean execution, created no artifact, and returned a clear `Invalid` result with `Stream has ended unexpectedly`. It failed only the old evaluator requirement that the final answer repeat the parser's stderr phrase `EOF marker not found`. The V1 contract now checks the user result `Invalid`, not one library message.
- The focused `invalid-document` rerun with the V1 result contract returned `small_stress_passed` in 61.2 seconds. Report: `packages/eval/.generated/stress/small-2026-08-13T15-26-55.311Z.json`. It used one clean execution, returned `Invalid`, created no artifact, had no inference failure, and kept a valid audit chain. Together with the three passing cases in the prior four-case report, this completes the short V1 stress evidence without another complete model sweep.

### Favorite-question protocol recovery — 2026-08-13

- Local session `9b4125e2-5ccf-41fe-9c74-300db47ff67b` supplied exact pre-fix real-model evidence. Gemma emitted call `call_msrq3x7i_3` with one valid favorite-fruit question array, then appended `}}<tool_call|><tool_call|>` and private continuation text inside the flat `questions` string. Core rejected the complete argument as a protocol-control transition. The run used later recovery and compaction turns, returned the choices as final text, and never published a pending question.
- Core now removes only that exact leaked tool-close suffix from the `question` string. It then applies the canonical nested question schema and stores the clean call in the next model request. Protocol transitions inside question data and transitions in all other tool arguments remain invalid. The regression uses the saved call ID and suffix, and its negative case proves that the general protocol guard stays active.
- The post-fix physical Windows fixture used the pinned Gemma model, current-user daemon, signed native inference worker, and Windows no-NIC guest. The full guest evidence passed Python, Node, document libraries, persistence, repair, cancellation, timeout, output, process, memory, quota, crash, symlink, network, credential, and host-path checks.
- Gemma issued the typed `question` tool. `agent.get` exposed the pending request, and `agent.answerQuestion` accepted `Apple`. Durable events were `question.asked` followed by `question.answered`; the run then returned a successful answer, the audit chain verified, and the trace ended with `accepted_tool_calls` then `accepted_response`.
- The ignored complete report is `packages/eval/.generated/reports/question-favorite-2026-08-13T16-54-42.932Z.json`. The running desktop debug bundle was owned by `Administrators`, so the signed AppContainer launcher could not replace the per-file worker ACL for a second headless process. The fixture used an ignored current-user-owned byte-for-byte copy; 3,531 files and 1,330,443,434 bytes matched, and the worker, runtime, and launcher SHA-256 hashes matched before the run.
- Focused protocol and question coverage passed 21 tests across three files. The complete Windows `pnpm verify` gate passed with an ignored Cargo target outside the running desktop's locked debug folder: source limits, lint, TypeScript, 429 unit tests with two declared skips, two native tests, signed helper and sidecar checks, Rust checks, resource validation, and the desktop production build passed.
- Follow-up session `28e831a5-15bc-412f-b8c9-ae6f98a0caf1` failed before question parsing. Its two inference turns ended in 5-12 ms with audit code `internal`. The development desktop had started at 19:16 with the pre-fix Core. The later verification build completed at 20:08 and its final development cleanup removed the 6,975,877,728-byte model from the live Tauri target resource tree. The follow-up at 20:21 could not stage that missing file. This failure was caused by the verification process, not by a tested post-fix binary.
- After the pinned model was restored, that same old Core reached call `call_msrtf8an_1` and reproduced the saved leaked `}}<tool_call|><tool_call|>` suffix. As expected, the old Core rejected the tool and returned the choices as plain text. The final compiled sidecar, SHA-256 `bf8018bd2115209076353a6d696f4b48b36754f1b9c94084baaf8234c7e0f852`, then ran the exact prompt with the owner-readable byte-identical packaged worker resources. It published the four-option Favorite Fruit picker, accepted `Apple (Recommended)`, retained `question.asked` then `question.answered`, and ended `succeeded` in 176.4 seconds.
- A separate real cold-cycle fixture first returned `READY`, unloaded the resident model through `model.unload`, then ran the exact favorite-question request. The pinned model reloaded, the Windows no-NIC guest evidence passed, the picker opened, `Apple` was accepted, and the run ended `succeeded` in 412.1 seconds. The ignored report is `packages/eval/.generated/reports/question-favorite-2026-08-13T17-36-39.427Z.json`.
- `desktop:check` no longer deletes the transient model under `target/debug` because that path can be in use by a running development desktop. Normal development-process exit and the explicit cleanup command retain ownership of that cleanup.
- Session `16c5654c-2a18-4cc7-8f27-a4b8bfd123a6` exposed an independent latency bug after the picker fix. Gemma used 47.6 seconds on a 2,048-token exhausted turn and 103.2 seconds on an 8,192-token exhausted turn. Core also spent 22.2 seconds on two anchored summaries even though prompt use was about 1,100 of 65,536 tokens. Two raw tool-call responses were rejected before the valid question call arrived; the picker itself opened in 4.7 seconds, and the post-answer response took 0.5 seconds.
- OpenCode uses a 32,000-token default output ceiling, capped by the selected model's declared output limit. A physical Vault Desk control proved that value is not suitable for the pinned local Gemma worker: with the model ready and a 65,536-token context, the exact favorite-question turn consumed 32,000 tokens before recovery, used 32,232 output tokens in total, and completed in 416.5 seconds. The ignored report is `packages/eval/.generated/reports/question-favorite-2026-08-14T05-01-50.595Z.json`.
- Native chat now uses a 16,384-token ceiling while reserving at least 4,096 tokens for the prompt. This doubles the former 8,192-token limit without accepting the measured seven-minute 32K failure. An output-only failure no longer compacts or removes typed tools. Core gives one direct recovery turn and stops on a second exhausted turn. An explicit question-tool test now makes the first turn one question call without a planning turn. Focused regression coverage uses the saved low-context favorite-question shape and proves no `Condensing the working context.` event is emitted before the recovered question.
- The final packaged Windows worker ran the exact favorite-question prompt with the model already ready and the final strict question-test prompt. The first 16,384-token chat turn issued the typed question in 4.5 seconds, the automatic answer selected `Mango`, and the final response completed the two-turn run in 5.3 seconds with 175 prompt tokens and 322 output tokens. There were no compaction or rejected-protocol turns, the allocated context was 65,536 tokens, and the audit chain verified. The ignored report is `packages/eval/.generated/reports/question-favorite-2026-08-14T05-39-13.041Z.json`; the earlier 32K control report contains the passing Windows no-NIC guest evidence.

- A task-specific physical Apple-silicon reproduction used the real pinned Gemma worker, current-user daemon, and no-NIC guest. The task intentionally left a file value undecided and required the primary agent to ask before executing code.
- Gemma issued the typed `question` tool, the active `agent.get` snapshot exposed one pending request, and `agent.answerQuestion` accepted `Alpha` while the run remained live. Durable events were exactly `question.asked` followed by `question.answered`.
- After the answer, Gemma continued the same run, executed Python successfully inside the no-NIC guest, created `/workspace/question-result.txt`, and returned `I have created the file /workspace/question-result.txt with the value Alpha.` The run state was `succeeded`, the audit chain verified, and trace outcomes were two `accepted_tool_calls` followed by `accepted_response`.
- The ignored complete report is `packages/eval/.generated/reports/question-macos.json`. This is focused macOS evidence only; packaged visual composition and Windows behavior remain separate evidence.
- The initial canonical nested question parameter schema caused the pinned model to append protocol punctuation to otherwise valid, unrelated Python calls. A stopped small-suite attempt reached 13 XLSX executions, 9 DOCX executions, and more than 8 PDF executions despite independently correct XLSX and DOCX artifacts. No execution, context, timeout, workspace, or isolation limit was raised.
- Focused model-facing schema trials reduced XLSX from 17 executions with the nested schema to 7 with a shallow object schema and 2 with a flat unconstrained item schema. The final adapter exposes one string containing the question JSON array while retaining canonical nested validation inside Core. After also documenting pypdf's required leading slash for metadata keys, focused XLSX and PDF cases each passed in 2 executions. The latest focused clarifying-question reproduction above passed against this final adapter.
- The final full small, context-session, scaled sequential, and scaled concurrent suites were not completed after the adapter change. They are explicitly handed to physical Windows with NVIDIA; macOS focused passes and the earlier canonical gate are not Windows or full-suite evidence. Windows must run `pnpm verify`, `pnpm test:m3:windows`, `pnpm test:m3:windows:memory`, `pnpm test:stress:m3:small`, `pnpm test:stress:m3:context-session`, `pnpm test:stress:m3:scaled:sequential`, and `pnpm test:stress:m3:scaled:concurrent`, inspect generated artifacts and audit evidence independently, and append the exact results here.
- Final non-stress verification passed: `pnpm verify` completed source limits, lint, TypeScript, 99 unit files with 402 tests passed and one declared skip, two native tests, Rust checks, helper and sidecar builds, resource validation, and the desktop production build. Pure helper and server-rendered component tests cover selection, paging, skipped answers, the Recommended badge, full-description hover text, and Stop reachability. Interactive desktop composition, keyboard behavior, and packaged visual composition remain not run.
- The first two canonical macOS gate attempts reproduced the same deterministic 4,081-token raw tool-call markup as response text in one concurrent execution-only run, with no VM start. Core now rejects protocol-control fragments as `rejected_unbacked_response`, removes the invalid assistant message, and retries without recording the same trace as accepted tool calls. The focused trace regression passed.
- Before the final question-schema adapter change, `pnpm test:m3:macos` returned `classification: certified`: real Python and Node each completed two executions and one artifact, maximum overlapping VMs was two, all confinement and bounded-resource checks passed, cleanup passed, and the allocated context was 131,072 tokens. This historical command emits terminal evidence rather than a JSON report and is not final-adapter stress certification.

### Collapsed workbook and result-table cycle — 2026-08-05

- A task-specific physical Apple-silicon reproduction used the real pinned Gemma worker, current-user daemon, and no-NIC guest against the original 36-workbook corpus. This is macOS evidence only; source paths and row contents remain in ignored local evidence.
- The generation task processed all 36 workbooks, found 17 matches, created exactly one 5,370-byte XLSX with worksheet dimension `A1:B18`, returned `Total matches found: 17`, and preserved a valid audit chain.
- The immediate table follow-up succeeded in one clean Python execution with zero stderr, returned exactly 17 GFM data rows, and reused the already verified complete corpus coverage. Core normalized overflow Markdown delimiters into the final cell instead of accepting malformed columns or requiring an unverified prose response.
- Non-qualifying iterations retained exact failures for collapsed `A1:A1` source metadata, `reset_dimensions()` on a normal worksheet, invalid table escape syntax, malformed GFM columns, omitted structured calls, duplicate repair, and an independently observed resource-contention timeout. No context, execution, or isolation limit was raised.
- Focused XLSX, prompt, output-contract, and combined source-recovery coverage passed: 44 tests across six files. `pnpm verify` passed with source limits, lint, TypeScript, 373 unit tests with one skip, two native tests, Rust checks, native helpers, sidecar validation, and the desktop build.

### Generated workbook delivery cycle — 2026-08-05

- A task-specific physical Apple-silicon reproduction used the real pinned Gemma worker, current-user daemon, and no-NIC guest against a 36-workbook source corpus. This is macOS evidence only.
- Before the fix, the guest processed all 36 workbooks and created the requested XLSX, but final response validation replaced the valid artifact-only completion with an empty response and ended the run with `agent_run_failed`.
- After the fix, the identical task succeeded in 82.323 seconds with an accepted final response, exactly one 5,369-byte XLSX deliverable, no checkpoint artifact, and a valid audit chain.
- Focused output-contract, artifact-declaration, and store coverage passed: 16 tests across four files. The checks cover preservation of generated-workbook final responses and exclusion of internal checkpoint files from current and historical user-visible artifact listings.
- `pnpm verify` passed with 366 unit tests, one skip, two native tests, TypeScript checks, Rust checks, native helpers, sidecar packaging, source limits, and the desktop build.

### Source discovery recovery cycle — 2026-08-03

- Focused loop and prompt tests passed: 16 tests across `loop-shell-recovery.test.ts` and `prompt-library.test.ts`.
- `pnpm verify` passed with 331 unit tests, one skip, two native tests, desktop builds, native helpers, and Rust checks.
- The realistic skill runner was repeated twice. Terminal-only passed both times. Combined source discovery also passed both times with `SOURCE_FILE=/source/src/pricing-rules.ts` and `SURCHARGE_BPS=275`, then reached workbook analysis without a guessed path or duplicate stall. The still-expected combined XLSX corpus/total limit remained for the aggregate-guidance commit. Reports: `packages/eval/.generated/stress/realistic-skills-2026-08-03T14-46-03.506Z.json` and `packages/eval/.generated/stress/realistic-skills-2026-08-03T14-50-38.346Z.json`.
- The small suite preserved all previous passes, including all three concurrent cases with `maximumRunning=3`. Invalid PDF and sequential XLSX-folder totals remained the planned later failures. Report: `packages/eval/.generated/stress/small-2026-08-03T14-55-01.388Z.json`.
- Scaled sequential and concurrent runs preserved the separately reported XLSX-folder aggregate limit; the one-million-row workbook and mixed 50-file workflow passed in both modes. Reports: `packages/eval/.generated/stress/scaled-sequential-2026-08-03T15-01-11.980Z.json` and `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T15-16-33.806Z.json`.
- `pnpm test:m3:macos` remained certified with real Python and Node artifacts, persistence, cancellation, limits, no-NIC confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token context cap. This command emits terminal evidence rather than a JSON report.

### Independent Windows Stage 2 physical check — 2026-08-21

- `pnpm desktop:build-sidecar` completed with exit code 0. The command did not emit an elapsed duration. It emitted target `x86_64-pc-windows-msvc`, Node `v24.18.0`, signing mode `windows-ephemeral-self-signed`, sidecar SHA-256 `801dff199c8ac46ed38fcdfeb47220d260d579d7aa07cc3fa057a033352405cc`, bundle SHA-256 `92e46dfbe2e3f7538899fc0d2cbd2b5fbf4e1e2864d201eee2c361733459a781`, and resource-manifest SHA-256 `2005af4bf092f946d6b5f618bbc5aa02e94b7f5ed257a984fd059832ae0ca309`. The staged resource manifest had `LastWriteTimeUtc` `2026-08-21T19:02:05.6246866Z`.

- Focused unit command: `pnpm test -- packages/core/src/agent/generic-read.test.ts packages/core/src/agent/generic-tools.test.ts packages/core/src/agent/tool-output.test.ts packages/core/src/agent/markdown-definition-library.test.ts packages/core/src/agent/session-summary.test.ts packages/core/src/agent/service-session-summary.test.ts`. Result: six test files and 56 tests passed in 966 ms.

- Real stress command: `pnpm test:stress:m3:context-session`. The command ended with exit code 1 and classification `context_session_limit_found`. Report-clock duration was 68.268 seconds, from report-path timestamp `2026-08-21T19:02:02.888Z` to report `createdAt` `2026-08-21T19:03:11.156Z`; it was within the ten-minute cap. No manual stop was used.

- Run, allocation, anchor, decision, and busy evidence: all seven runs ended `succeeded` with `error: null`: `44b06455-8ea8-41e4-b8eb-8795cb623d03`, `c081ecc4-55dc-404a-bafb-cd6af06b81b2`, `9271bb82-f5a1-45a4-a229-02b13e239fc3`, `4ca70e59-5e57-49ab-b42e-891e0bbf3cc5`, `ba5e3ade-af60-4f0b-9a6d-58437e4de031`, `28bccc51-3c48-4bbd-918c-c8189836c839`, and `fae34573-c8d4-423a-922d-60de210689be`. All seven allocations were 65,536 tokens. No `agent_busy` error occurred. Every turn recorded `anchored: false`; `distinctAnchors` was 0, but the case requires at least 3. `missingTerms` was empty. The final response retained review style `Concise`, unresolved item `vendor approval`, next move `Compare the revised draft`, privacy preference `Keep all work offline`, and delivery format `Plain text`.

- Sidecar selection evidence and limit: the Windows resolver prefers the release bundle, but that root was absent after the run. The stress runtime therefore used the staged development inference root `packages/desktop/src-tauri/resources/core/inference`; its recorded hardware-worker SHA-256 was `80c6d4bceab52d0475f55eba1fe93f5288bba9de0ddc0c44f193ce9a76cf3272`, Node runtime SHA-256 was `61cd9e8caa61df0bf662d730990ca5728cee06fa1e88dbfbd9cd2178c1689c51`, and worker SHA-256 was `b04063de880abe8c89fe8ef0dfeb7e68e0b8a30770740109f71c5a5e2fb79ec3`. The report path was created before the build resource-manifest timestamp, and the runner did not emit its sidecar-selection time. The completed rebuilt sidecar use cannot be proved from this run.

- Report: `packages/eval/.generated/stress/context-session-2026-08-21T19-02-02.888Z.json`. Stage 2 conclusion: not ready. The unit files passed, but real stress failed the required anchor count. This run is not M3 certification evidence.

### Independent Windows Stage 1 check — 2026-08-21

- Focused unit command: `pnpm test -- packages/core/src/agent/generic-read.test.ts packages/core/src/agent/generic-tools.test.ts packages/core/src/agent/tool-output.test.ts packages/core/src/agent/markdown-definition-library.test.ts`. Result: four test files and 35 tests passed in 637 ms.

- Real stress command: `pnpm test:stress:m3:small -- --case terminal-discovery`. Platform: `win32-x64`. Duration: 50.052 seconds. Result: `small_stress_passed`; terminal state `succeeded`; response `SURCHARGE_BPS=275`; audit validation passed. The runner ended 249.948 seconds before its five-minute deadline. Report: `packages/eval/.generated/stress/small-2026-08-21T17-48-06.156Z.json`.

- Tool and bound evidence: one `list` call with `depth: 2` and `path: /source` found `/source/pricing-rules.ts`. One `read` call with `offset: 1`, `limit: 2000`, and the exact source path read `surchargeBasisPoints = 275`. Both calls completed. No bound rejection occurred, so bound recovery was not needed. The run used zero guest program executions, zero failed executions, zero execution milliseconds, zero inference failures, zero context compactions, no artifact, and no error.

- Not run: no second stress attempt, full small suite, context-session suite, scaled suites, `pnpm test:m3:windows`, or `pnpm verify`. The directed scope allowed one focused real stress case only. Conclusion: ready for the Stage 1 focused check; this result is not M3 certification.

### Focused Windows Stage 1 retest — 2026-08-21

- Command: `pnpm test:stress:m3:small -- --case terminal-discovery`. Case: `terminal-discovery` (plain UTF-8 text). Duration: 164.613 seconds. Result: `small_stress_limit_found`; terminal state `succeeded`, but the required `SURCHARGE_BPS=275` token was missing. The run made zero guest program executions and had one inference failure. Failure evidence: after `list` found `/source/pricing-rules.ts`, `read` failed repeatedly with `Error: invalid_limit` and `grep` failed repeatedly with `Error: invalid_include`; the run reached the 24 guest-execution limit. The final response said it could not retrieve the value. Audit validation passed. Report: `packages/eval/.generated/stress/small-2026-08-21T17-30-48.723Z.json`.

- Command: `pnpm test:stress:m3:small -- --case invalid-document`. Case: `invalid-document` (truncated PDF). Duration: 71.585 seconds. Result: `small_stress_passed`; terminal state `succeeded`; expected `invalid` evidence was present; audit validation passed. The run used two bounded Python executions (519 ms total), with one failed execution and no inference failure. Failure evidence: the first bounded Python program stopped with `SyntaxError: unterminated f-string literal`; the replacement program completed, reported `truncated.pdf` as invalid with `Stream has ended unexpectedly`, and emitted the bounded `EOF marker not found` parser diagnostic. This failure occurred during bounded program execution, not before or at `read`, and later model completion succeeded. Report: `packages/eval/.generated/stress/small-2026-08-21T17-34-18.710Z.json`.

### Focused Windows Stage 1 read stress — 2026-08-21

- Command: `pnpm test:stress:m3:small -- --case terminal-discovery`. Case: `terminal-discovery` (plain text). Duration: 80.857 seconds. Result: `small_stress_passed`; terminal state `succeeded`; expected `SURCHARGE_BPS=275` was present; audit validation passed. The run used two guest executions (11 ms total); one failed execution was recorded, with `error: null`, and the case still passed. Report: `packages/eval/.generated/stress/small-2026-08-21T17-12-21.274Z.json`.

- Command: `pnpm test:stress:m3:small -- --case romanian-task`. Case: `romanian-task` (accented task text and binary XLSX input). Duration: 69.564 seconds. Result: `small_stress_limit_found`; terminal state `succeeded`, but the case failed because response token `TOTAL_AVANS=12009` was missing. The run loaded `xlsx-workbooks`, used two guest executions (1,291 ms total), had no failed execution, no inference failure, and `error: null`. Report: `packages/eval/.generated/stress/small-2026-08-21T17-14-21.752Z.json`.

- Command: `pnpm test:stress:m3:small -- --case invalid-document`. Case: `invalid-document` (invalid truncated PDF input). Duration: 5 minutes; the runner stopped it at its declared deadline. Result: failed with `Error: Stress cases timed out after 5 minutes.` The last progress event was at 296.291 seconds during `Retrying the local model once`; it had three guest executions. The runner wrote no terminal classification or JSON report.

- Trace detail: in `terminal-discovery`, `read` rejected model argument `limit: 1000000000000000` with `Error: invalid_limit`, as required by the numeric range contract. The case then recovered with a bounded search. In `romanian-task`, the recorded response was `TOTAL_AVANS=12009.0`; the strict oracle required `TOTAL_AVANS=12009`.

### Focused Windows Stage 1 final retest — 2026-08-21

- Focused unit command: `pnpm test -- packages/core/src/agent/generic-read.test.ts packages/core/src/agent/generic-tools.test.ts packages/core/src/agent/tool-output.test.ts packages/core/src/agent/markdown-definition-library.test.ts`. Result: four test files and 35 tests passed in 687 ms. The first restricted-shell attempt stopped during Vitest configuration with `spawn EPERM`; it did not discover or run a test. The same command then passed outside the restricted shell.

- Real stress command: `pnpm test:stress:m3:small -- --case terminal-discovery`. Platform: `win32-x64`. Duration: 87.981 seconds. Result: `small_stress_limit_found`; terminal state `failed`; error `agent_run_failed`; required `SURCHARGE_BPS=275` was missing. The case used zero guest executions, zero failed guest executions, and two inference failures. Audit validation passed. The runner ended 212.019 seconds before its five-minute deadline. Report: `packages/eval/.generated/stress/small-2026-08-21T17-40-20.753Z.json`.

- Tool-error recovery evidence: three `list` calls failed with `Error: invalid_depth: use an integer from 0 to 8`. A later bounded list call completed, and the next list call found `/source/pricing-rules.ts`. Four `read` calls then failed with `Error: invalid_limit: use an integer from 1 to 2000`; no later call read the file or recovered the required value.

- Not run: no second stress attempt, full small suite, context-session suite, scaled suites, `pnpm test:m3:windows`, or `pnpm verify`. The directed scope allowed one focused real stress case only. Conclusion: not ready.

### Invalid PDF clean-stop cycle — 2026-08-03

- Focused PDF routing and attachment prompt tests passed: 14 tests across `prompt-library.test.ts` and `loop-attachments.test.ts`.
- `pnpm verify` passed with 332 unit tests, one skip, two native tests, desktop builds, native helpers, and Rust checks.
- The realistic skill suite preserved the source-discovery fixes, PDF and XLSX-only passes, skill routing, and audit validity. Combined XLSX totals remained the planned aggregate-guidance failure. Report: `packages/eval/.generated/stress/realistic-skills-2026-08-03T15-31-01.178Z.json`.
- The small suite was repeated twice. Invalid PDF passed both times with exactly one execution, `INVALID_DOCUMENT_STOP=1`, exit code 0, no repair, and no artifacts. `pypdf` emitted the bounded `EOF marker not found` parser diagnostic before the caught exception. The sequential XLSX-folder total remained the only small-suite failure; all previous and concurrent cases passed with `maximumRunning=3`. Reports: `packages/eval/.generated/stress/small-2026-08-03T15-36-30.587Z.json` and `packages/eval/.generated/stress/small-2026-08-03T15-42-36.222Z.json`.
- Scaled sequential preserved the XLSX-folder aggregate limit while the one-million-row workbook and mixed workflow passed. Report: `packages/eval/.generated/stress/scaled-sequential-2026-08-03T15-48-39.833Z.json`.
- The first scaled-concurrent run had a stochastic one-workbook total miss while the 50-file XLSX and mixed cases passed; the identical repeat restored the previous profile with workbook and mixed passes and only the known XLSX-folder aggregate limit. Reports: `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T16-03-27.691Z.json` and `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T16-13-52.846Z.json`.
- `pnpm test:m3:macos` remained certified with real Python and Node artifacts, persistence, cancellation, limits, no-NIC confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token context cap. This command emits terminal evidence rather than a JSON report.

### Workbook aggregate and combined-workflow cycle — 2026-08-03

- Focused XLSX, source-recovery, rejection, and prompt tests passed: 28 tests across `loop-xlsx.test.ts`, `loop-shell-recovery.test.ts`, `loop-source-recovery.test.ts`, and `prompt-library.test.ts`.
- `pnpm verify` passed with 335 unit tests, one skip, two native tests, desktop builds, native helpers, and Rust checks.
- The final realistic skill suite passed twice consecutively. Terminal-only and combined source discovery returned `/source/src/pricing-rules.ts` and `SURCHARGE_BPS=275`; XLSX-only and combined processing used the complete case-insensitive corpus; combined returned `XLSX_MATCHES=4`, `XLSX_TOTAL=6006`, and `q2-audit.md`. Both reports retained `maximumRunning=5`, correct skill routing, and a valid audit chain. Reports: `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-29-39.052Z.json` and `packages/eval/.generated/stress/realistic-skills-2026-08-03T18-15-31.820Z.json`.
- The final small suite passed twice consecutively. Sequential and concurrent XLSX-folder cases both returned `XLSX_MATCHES=6` and `XLSX_TOTAL=12009` in one execution; the one-workbook and mixed totals remained correct; invalid PDF still stopped cleanly in one execution; concurrent runs retained `maximumRunning=3`. Reports: `packages/eval/.generated/stress/small-2026-08-03T17-34-49.093Z.json` and `packages/eval/.generated/stress/small-2026-08-03T18-20-51.953Z.json`.
- Scaled sequential passed the 100-page PDF, one-million-row workbook, 50-workbook, and mixed 50-file workflows. The workbook folder returned `XLSX_MATCHES=500` and `XLSX_TOTAL=12752750`; mixed returned `XLSX_MATCHES=200`, `XLSX_TOTAL=2101100`, `WORD_PAGES=3000`, and `WORD_CHECKSUM=46651500`. Report: `packages/eval/.generated/stress/scaled-sequential-2026-08-03T17-39-20.658Z.json`.
- Scaled concurrent passed the one-million-row workbook, 50-workbook, and mixed workflows with `maximumRunning=3` and the same exact totals. The 50-workbook run used four executions and the mixed run used all six; long batches still exceeded the intended 75-second work window. Report: `packages/eval/.generated/stress/scaled-concurrent-2026-08-03T18-00-08.345Z.json`.
- The non-qualifying iterations exposed and retained exact evidence for a case-sensitive uppercase-XLSX miss, overcomplicated small-corpus checkpoint repair, guessed source extension allowlists, and an overlong combined recovery. Reports: `packages/eval/.generated/stress/realistic-skills-2026-08-03T16-30-56.384Z.json`, `packages/eval/.generated/stress/small-2026-08-03T16-45-16.946Z.json`, `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-01-57.566Z.json`, `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-11-59.149Z.json`, and `packages/eval/.generated/stress/realistic-skills-2026-08-03T17-18-34.096Z.json`.
- `pnpm test:m3:macos` remained certified with real Python and Node artifacts, persistence, cancellation, limits, no-NIC confinement, cleanup, two overlapping VM lifetimes, and the 131,072-token context cap. This command emits terminal evidence rather than a JSON report.

### Independent Windows Stage 2 context-session check — 2026-08-21

- Focused unit command: `pnpm test -- packages/core/src/agent/session-summary.test.ts packages/core/src/agent/service-session-summary.test.ts packages/core/src/agent/chat-inference-recovery.test.ts`. Result: three test files and 22 tests passed in 819 ms. The first restricted-shell attempt stopped during Vitest configuration with `spawn EPERM`; it did not discover or run a test. The same command then passed outside the restricted shell.

- Real stress command: `pnpm test:stress:m3:context-session`. Result: failed with exit code 1 and `Error: Stress session remained busy after its previous terminal run.` The runner stopped at `startRun` in `packages/eval/src/stress/m3-session-runtime.ts`; it waited for `agent.start` until its internal deadline after a previous run remained busy.

- Duration and stop: the runner did not emit a process duration. The generated model-store record was written at `2026-08-21T17:58:25.890Z`; command exit was observed at `2026-08-21T18:19:21.971Z`, an elapsed observation of 20 minutes 56.081 seconds. This exceeded the assigned 20-minute budget. The command exited by itself before a manual stop could occur. This result is a time-budget failure, not a product pass.

- Terminal state, anchors, decisions, and trace details: not available. The runner wrote no `context-session-*.json` report and emitted no per-turn progress. It did not return an `agent.get` terminal snapshot or an `agent.trace` record. The count of anchors and retained decisions is therefore not available.

- Conclusion: not ready. Do not treat this run as M3 context-session evidence.

### Independent Windows Stage 2 context-session retest — 2026-08-21

- Focused unit command: `pnpm test -- packages/core/src/agent/generic-read.test.ts packages/core/src/agent/generic-tools.test.ts packages/core/src/agent/tool-output.test.ts packages/core/src/agent/markdown-definition-library.test.ts packages/core/src/agent/session-summary.test.ts packages/core/src/agent/service-session-summary.test.ts`. Result: failed during Vitest startup. `vitest.workspace.ts` did not load because Vite returned `Error: spawn EPERM`. The command did not discover or run a test file. The required command was not run again.

- Real stress command: `pnpm test:stress:m3:context-session`. Platform: `win32-x64`; total memory: 33,408,679,936 bytes. The command ended with exit code 1 and classification `context_session_limit_found`. No time-budget stop was needed. The command output did not give one total duration. The observed process-session times were 30.0023799 seconds before the session output and 26.750645 seconds to terminal exit, for 56.7530249 seconds. The generated report path time is `2026-08-21T18:34:42.380Z`; its `createdAt` value is `2026-08-21T18:35:43.763Z`, a 61.383-second interval. Both values are below the 15-minute limit.

- Session-busy and terminal behavior: no `agent_busy` error occurred. All seven runs ended `succeeded` with `error: null`. Run IDs were `28e6df04-da83-4769-8c64-beeef39fd83c`, `cad3806b-53a4-446d-bf99-af614c5af075`, `a6f45bbc-d07e-44fb-b768-ed87cf40f5ac`, `41926864-b287-4f92-a136-f2e3dac5b3df`, `4cfe1f45-fd7a-4cab-9615-c7e2111bb1a7`, `9d2fa473-1ae4-46a0-92aa-38a830ccf5ac`, and `08c6b727-0dd2-4751-828c-f0342bf46436`.

- Anchor, decision, and trace evidence: the run used a 65,536-token context on all seven trace turns. Prompt character counts were 11,587, 5,908, 81,332, 10,449, 81,469, 10,577, and 12,486. Every turn recorded `anchored: false`; `distinctAnchors` was 0, but the case requires at least 3. `missingTerms` was empty. The final response retained all required decisions: review style `Concise`; unresolved item `vendor approval`; next move `Compare the revised draft`; privacy preference `Keep all work offline`; delivery format `Plain text`.

- Report: `packages/eval/.generated/stress/context-session-2026-08-21T18-34-42.380Z.json` was written. It contains the run, context, anchor, and final-decision results. It does not contain full ordered `agent.trace` records. No report is missing.

- Conclusion: not ready. The focused unit check did not run, and the real stress check failed its required anchor count. Do not treat this retest as M3 context-session evidence.

### Independent Windows Stage 1–2 final check — 2026-08-21

- Focused unit command: `pnpm test -- packages/core/src/agent/generic-read.test.ts packages/core/src/agent/generic-tools.test.ts packages/core/src/agent/tool-output.test.ts packages/core/src/agent/markdown-definition-library.test.ts packages/core/src/agent/session-summary.test.ts packages/core/src/agent/service-session-summary.test.ts`. Result: six test files and 54 tests passed in 1.13 seconds.

- Real stress command: `pnpm test:stress:m3:context-session`. Report-clock duration was 60.170 seconds, from the report-path timestamp `2026-08-21T18:44:03.172Z` to `createdAt` `2026-08-21T18:45:03.342Z`; this was within the 10-minute cap. The command ended with exit code 1 and result `context_session_limit_found`.

- Run and session-busy evidence: all seven runs ended `succeeded` with `error: null`: `7a58119f-1eb5-46bd-8d5e-d9cd8d91554f`, `08c8fd84-09b4-429c-acd7-37fca38f1eb6`, `94d7b5a9-8d58-4b91-954a-726615e3a80b`, `b6ff3e83-70a0-4334-9e35-8ac535439fb2`, `09c0b1a8-e559-42d7-bc84-bcdd2cdee428`, `89b6e238-dfed-4ad0-9115-d866ddaaf88f`, and `8fee5b85-2ccf-4b2f-bac1-333da3687dda`. No `agent_busy` error occurred.

- Allocation, anchor, and decision evidence: all seven measured allocations were 65,536 tokens. Every trace prompt recorded `anchored: false`; the distinct-anchor count was 0, but the case requires at least 3. `missingTerms` was empty. The final response retained review style `Concise`, unresolved item `vendor approval`, next move `Compare the revised draft`, privacy preference `Keep all work offline`, and delivery format `Plain text`.

- Trace and report path: `packages/eval/.generated/stress/context-session-2026-08-21T18-44-03.172Z.json`. This report contains run states, allocations, anchors, and final decisions. It does not contain complete ordered `agent.trace` records, and the runner writes no separate trace file.

- Stage 1–2 evidence conclusion: not ready. The Stage 1–2 unit checks passed, but the Stage 2 real stress case failed its required anchor count. Do not treat this check as M3 certification evidence.

### Independent Windows Stage 2 anchor-evidence check — 2026-08-21

- Focused unit command: `pnpm test -- packages/core/src/agent/generic-read.test.ts packages/core/src/agent/generic-tools.test.ts packages/core/src/agent/tool-output.test.ts packages/core/src/agent/markdown-definition-library.test.ts packages/core/src/agent/session-summary.test.ts packages/core/src/agent/service-session-summary.test.ts`. Result: six test files and 55 tests passed in 1.21 seconds.

- Real stress command: `pnpm test:stress:m3:context-session`. Report-clock duration was 59.268 seconds, from the report-path timestamp `2026-08-21T18:51:31.386Z` to `createdAt` `2026-08-21T18:52:30.654Z`; this was within the ten-minute cap. The command ended with exit code 1 and classification `context_session_limit_found`. No time-cap stop was needed.

- Run state and session-busy evidence: all seven runs ended `succeeded` with `error: null`: `6b60b1b9-d95b-4a9c-a3e8-5480cfe4f4a6`, `de9fa1a7-7e34-45a6-beb7-87a32d4bed84`, `2b5bff45-0f0a-41af-a1f6-25a4476473f6`, `a8151474-715c-45f3-a11c-2884b2abdbbe`, `c9283d61-7806-4970-931c-8b16e660292c`, `5f97e34f-4163-4f71-98eb-d2344ea0224b`, and `6c7fe77b-3901-415e-b0e4-be1594609d63`. No `agent_busy` error was emitted.

- Allocation, anchor, and decision evidence: all seven allocations were 65,536 tokens. Every turn recorded `anchored: false`; `distinctAnchors` was 0, but the case requires at least 3. `missingTerms` was empty. The final response retained review style `Concise`, unresolved item `vendor approval`, next move `Compare the revised draft`, privacy preference `Keep all work offline`, and delivery format `Plain text`.

- Report: `packages/eval/.generated/stress/context-session-2026-08-21T18-51-31.386Z.json`. It contains the run states, allocations, anchors, and retained decisions. It does not contain complete ordered `agent.trace` records, and the runner writes no separate trace file.

- Stage 2 conclusion: not ready. The unit checks passed and the run did not remain busy, but the real stress case failed its required anchor count. Do not treat this check as M3 certification evidence.

### Independent Windows Stage 2 post-build check — 2026-08-21

- Prebuilt identity: the completed `pnpm desktop:build-sidecar` record ended with exit code 0 before this test. It recorded target `x86_64-pc-windows-msvc`, Node `v24.18.0`, signing mode `windows-ephemeral-self-signed`, sidecar SHA-256 `801dff199c8ac46ed38fcdfeb47220d260d579d7aa07cc3fa057a033352405cc`, bundle SHA-256 `92e46dfbe2e3f7538899fc0d2cbd2b5fbf4e1e2864d201eee2c361733459a781`, and resource-manifest SHA-256 `2005af4bf092f946d6b5f618bbc5aa02e94b7f5ed257a984fd059832ae0ca309`. The staged resource-manifest `LastWriteTimeUtc` was `2026-08-21T19:02:05.6246866Z`, before this test report path time of `2026-08-21T19:08:37.269Z`. No build command ran in this check.

- Focused unit command: `pnpm test -- packages/core/src/agent/generic-read.test.ts packages/core/src/agent/generic-tools.test.ts packages/core/src/agent/tool-output.test.ts packages/core/src/agent/markdown-definition-library.test.ts packages/core/src/agent/session-summary.test.ts packages/core/src/agent/service-session-summary.test.ts`. Result: six test files and 56 tests passed in 1.33 seconds.

- Real stress command: `pnpm test:stress:m3:context-session`. The command ended with exit code 1 and classification `context_session_limit_found`. Report-clock duration was 59.297 seconds, from report-path time `2026-08-21T19:08:37.269Z` to report `createdAt` `2026-08-21T19:09:36.566Z`. It was within the ten-minute cap. No time-cap stop was used.

- Run state and busy evidence: all seven runs ended `succeeded` with `error: null`: `646bd0de-829c-42cb-acfd-8a215b7fe002`, `206fa9cc-c068-47ad-872c-974c80d8f3d0`, `b82251a3-a83f-41f1-a20c-b175d519dadb`, `9bb4c383-acda-4b07-8248-b3b119941dea`, `6607e546-a5a2-45ca-b057-ba62543adc7d`, `4af64100-add6-40da-8722-1a213230d480`, and `a91fe0aa-5893-4df8-a0de-b91877182853`. No `agent_busy` error occurred.

- Allocation, anchor, and decision evidence: all seven allocations were 65,536 tokens. Every turn recorded `anchored: false`; `distinctAnchors` was 0, but the case requires at least 3. `missingTerms` was empty. The final response retained review style `Concise`, unresolved item `vendor approval`, next move `Compare the revised draft`, privacy preference `keep all work offline`, and delivery format `plain text`.

- Report: `packages/eval/.generated/stress/context-session-2026-08-21T19-08-37.269Z.json`. It contains the run states, allocations, anchors, and retained decisions. It does not contain complete ordered `agent.trace` records, and the runner writes no separate trace file.

- Stage 2 conclusion: not ready. The prebuilt sidecar identity and six focused unit files passed, but the real stress case failed the required anchor count. Do not treat this check as M3 certification evidence.
