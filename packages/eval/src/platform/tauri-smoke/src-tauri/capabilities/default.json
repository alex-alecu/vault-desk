{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core webview access only; no shell, network, process, or filesystem plugin commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
